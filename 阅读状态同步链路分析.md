# 用户阅读状态同步链路分析

## 一、整体架构概览

用户阅读状态（已读/已见/归档等）的同步是一个跨多终端、多服务的完整链路。系统采用 **"HTTP上报 + WebSocket广播 + 数据库持久化 + 最终一致性补齐"** 的架构模式。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              客户端 (Browser)                            │
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐  │
│  │  UI组件     │──▶│  SDK API    │──▶│  HTTP请求   │   │ WebSocket   │  │
│  │ (Bell/Inbox)│   │ (Novu类)   │   │  状态上报   │◀──│  实时监听   │  │
│  └─────────────┘   └─────────────┘   └─────────────┘   └─────────────┘  │
│         │                  │                  │                │          │
│         ▼                  ▼                  ▼                ▼          │
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐  │
│  │ 事件总线    │   │ 本地缓存    │   │ 乐观更新    │   │ 断线重连    │  │
│  │ (Emitter)   │   │ (Cache)     │   │ (Optimistic)│   │ (Reconnect) │  │
│  └─────────────┘   └─────────────┘   └─────────────┘   └─────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTPS / WSS
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              服务端 (Backend)                            │
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐  │
│  │  API服务    │   │  业务用例   │   │  WebSocket  │   │  队列服务    │  │
│  │ (inbox API) │──▶│ (UseCases) │──▶│  网关       │◀──│ (WS Queue)  │  │
│  │  接收上报   │   │  状态处理   │   │  广播推送   │   │  异步分发   │  │
│  └─────────────┘   └─────────────┘   └─────────────┘   └─────────────┘  │
│         │                  │                  │                │          │
│         ▼                  ▼                  ▼                ▼          │
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐  │
│  │ 数据访问层  │   │  缓存失效   │   │  在线状态   │   │  Webhook     │  │
│  │ (Repository)│   │ (Invalidate)│   │ (Online)    │   │ (Outbound)  │  │
│  └─────────────┘   └─────────────┘   └─────────────┘   └─────────────┘  │
│                                    │                                       │
│                                    ▼                                       │
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────┐                      │
│  │  MongoDB    │   │  Redis      │   │  ClickHouse │                      │
│  │  持久化     │   │  缓存层     │   │  数据分析   │                      │
│  └─────────────┘   └─────────────┘   └─────────────┘                      │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 二、各层职责与组织方式

### 1. 客户端 SDK 层 (`packages/js`)

#### 核心模块组织

| 模块 | 文件位置 | 核心职责 |
|------|----------|----------|
| **入口类** | `src/novu.ts` | SDK 统一入口，管理 Session、初始化各子模块 |
| **通知管理** | `src/notifications/` | 通知列表、状态操作、乐观更新 |
| **WebSocket** | `src/ws/socket.ts` | 实时连接管理、事件监听、断线重连 |
| **API 服务** | `src/api/inbox-service.ts` | HTTP 请求封装，与后端 API 交互 |
| **事件总线** | `src/event-emitter/` | 内部事件发布订阅，解耦各模块 |
| **本地缓存** | `src/cache/notifications-cache.ts` | 通知数据本地缓存，减少网络请求 |

#### 1.1 状态上报机制 (`src/notifications/helpers.ts`)

**核心逻辑：乐观更新 + 事件驱动**

```typescript
// 单个通知标记为已读
export const read = async ({ emitter, apiService, args }) => {
  // 1. 生成乐观更新值
  const optimisticValue = new Notification({ ...notification, isRead: true, readAt: now });
  
  // 2. 发射 pending 事件，UI 立即响应
  emitter.emit('notification.read.pending', { data: optimisticValue });
  
  try {
    // 3. 异步调用后端 API
    const response = await apiService.read(notificationId);
    
    // 4. 发射 resolved 事件，更新最终状态
    emitter.emit('notification.read.resolved', { data: updatedNotification });
  } catch (error) {
    // 5. 失败时回滚
    emitter.emit('notification.read.resolved', { error });
  }
};
```

**上报 API 接口：**

| 操作 | HTTP 方法 | 路径 | 说明 |
|------|----------|------|------|
| 标记单个已读 | PATCH | `/inbox/notifications/:id/read` | 单个通知 |
| 标记单个未读 | PATCH | `/inbox/notifications/:id/unread` | 单个通知 |
| 批量标记已见 | POST | `/inbox/notifications/seen` | 支持按 ID 或筛选条件 |
| 批量标记已读 | POST | `/inbox/notifications/read` | 支持按标签/数据筛选 |
| 批量归档 | POST | `/inbox/notifications/archive` | 支持按条件批量 |

#### 1.2 WebSocket 实时监听 (`src/ws/socket.ts`)

**连接管理：**
- 使用 `socket.io-client` 建立长连接
- 自动重连：`reconnectionDelayMax: 10000`
- 认证方式：URL Query 参数携带 JWT Token

**监听的实时事件：**

| 事件名 | 触发时机 | 数据结构 |
|--------|----------|----------|
| `notification_received` | 收到新通知 | `{ message: InboxNotification }` |
| `unseen_count_changed` | 未见计数变化 | `{ unseenCount: number, hasMore: boolean }` |
| `unread_count_changed` | 未读计数变化 | `{ unreadCount: number, counts: { total, severity }, hasMore }` |

#### 1.3 本地缓存与事件同步 (`src/cache/notifications-cache.ts`)

**缓存键设计：**
- 基于查询参数（tags、read、archived、severity 等）生成 JSON 字符串作为键
- 自动监听状态变更事件，更新缓存中的通知状态

**监听的更新事件：**
```typescript
const updateEvents = [
  'notification.read.pending',
  'notification.read.resolved',
  'notification.unread.pending',
  'notification.unread.resolved',
  'notifications.read_all.pending',
  'notifications.read_all.resolved',
  // ... 更多事件
];
```

---

### 2. API 服务层 (`apps/api`)

#### 核心模块组织

| 模块 | 文件位置 | 核心职责 |
|------|----------|----------|
| **控制器** | `src/app/inbox/inbox.controller.ts` | HTTP 路由定义、参数校验、鉴权 |
| **业务用例** | `src/app/inbox/usecases/` | 各操作的业务逻辑实现 |
| **DTO 定义** | `src/app/inbox/dtos/` | 请求/响应数据结构定义 |
| **工具类** | `src/app/inbox/utils/` | 数据映射、参数校验 |

#### 2.1 控制器层 (`inbox.controller.ts:1-858`)

**核心职责：**
- 路由定义与请求分发
- 鉴权校验（`AuthGuard('subscriberJwt')`）
- 参数转换与校验
- 调用对应 UseCase 执行业务逻辑

**关键端点示例：**
```typescript
// 单个通知标记为已读
@Patch('/notifications/:id/read')
async markNotificationAsRead(
  @SubscriberSession() subscriberSession,
  @Param('id') notificationId: string
) {
  return this.markNotificationAsUsecase.execute(
    MarkNotificationAsCommand.create({
      organizationId: subscriberSession._organizationId,
      subscriberId: subscriberSession.subscriberId,
      environmentId: subscriberSession._environmentId,
      contextKeys: subscriberSession.contextKeys,
      notificationId,
      read: true,
    })
  );
}
```

#### 2.2 业务用例层 (Use Cases)

采用 **命令模式 + 单一职责** 设计，每个操作用独立的 UseCase 处理。

##### 2.2.1 标记通知为已见 (`mark-notifications-as-seen.usecase.ts:1-280`)

**执行流程：**
1. 校验订阅者存在性
2. 按 ID 或筛选条件批量更新数据库
3. 记录交互轨迹（ClickHouse）
4. 失效缓存
5. 发送 WebSocket 队列任务广播计数变化
6. 触发 Webhook 通知外部系统

**关键代码片段：**
```typescript
async execute(command: MarkNotificationsAsSeenCommand): Promise<void> {
  // 1. 批量更新数据库（分批处理，每批50条）
  const BATCH_SIZE = 50;
  for (const idChunk of notificationIdChunks) {
    const batchResults = await this.messageRepository.updateMessagesStatusByIds({
      environmentId, subscriberId, ids: idChunk, seen: true,
    });
    updatedMessages.push(...batchResults);
  }

  // 2. 失效计数缓存
  await this.invalidateCache.invalidateQuery({
    key: buildMessageCountKey().invalidate({ subscriberId, _environmentId }),
  });

  // 3. 发送 WebSocket 广播任务
  this.webSocketsQueueService.add({
    name: 'sendMessage',
    data: {
      event: WebSocketEventEnum.UNSEEN,
      userId: subscriber._id,
      _environmentId,
      contextKeys,
    },
    groupId: subscriber._organizationId,
  });
}
```

##### 2.2.2 批量更新通知状态 (`mark-many-notifications-as.usecase.ts:1-261`)

处理 read/archive/snooze 等状态的批量更新，流程与已见类似，但支持更多状态字段。

##### 2.2.3 按条件全量更新 (`update-all-notifications.usecase.ts:1-182`)

支持按标签、数据等筛选条件批量更新所有匹配的通知。

---

### 3. 数据访问层 (`libs/dal`)

#### 3.1 消息仓库 (`repositories/message/message.repository.ts`)

**核心数据模型 (`message.entity.ts`)：**

```typescript
class MessageEntity {
  _id: string;
  _subscriberId: string;
  _environmentId: string;
  
  // 状态字段
  seen: boolean;           // 是否已见
  read: boolean;           // 是否已读
  archived: boolean;       // 是否已归档
  snoozedUntil?: string;   // 暂缓时间
  deleted: boolean;        // 软删除标记
  
  // 时间戳
  lastSeenDate?: Date;     // 最后见时间
  lastReadDate?: Date;     // 最后读时间
  firstSeenDate?: Date;    // 首次见时间
  archivedAt?: string;     // 归档时间
  deliveredAt?: string[];  // 投递时间
  
  // 关联字段
  channel: ChannelTypeEnum;  // 通道类型
  tags?: string[];           // 标签
  contextKeys?: string[];    // 上下文键（多租户隔离）
  data?: Record<string, unknown>;  // 自定义数据
}
```

#### 3.2 核心更新方法

##### `updateMessagesStatusByIds` (第751行)

按通知 ID 列表批量更新状态：

```typescript
async updateMessagesStatusByIds({
  environmentId, subscriberId, ids, seen, read, archived, snoozedUntil, contextKeys
}): Promise<MessageEntity[]> {
  const query = {
    _environmentId: environmentId,
    _subscriberId: subscriberId,
    _id: { $in: ids.map(id => new Types.ObjectId(id)) },
    ...(contextKeys?.length && { contextKeys: { $in: contextKeys } }),
  };
  
  return this.updateMessagesStatus({ query, seen, read, archived, snoozedUntil });
}
```

##### `updateMessagesFromToStatus` (第790行)

按筛选条件批量更新：

```typescript
async updateMessagesFromToStatus({
  environmentId, subscriberId, from, to, contextKeys
}): Promise<MessageEntity[]> {
  // from: 筛选条件（tags, data, seen, read, archived）
  // to: 目标状态
  let query = {
    _environmentId, _subscriberId,
    ...(contextKeys?.length && { contextKeys: { $in: contextKeys } }),
    ...from.tagGroups && buildTagsQuery(from.tagGroups),
    ...from.data && getFlatObject({ data: from.data }),
  };
  
  return this.updateMessagesStatus({ query, ...to });
}
```

##### `updateMessagesStatus` (第861行)

**状态联动逻辑：**

| 操作 | 自动联动的字段 |
|------|---------------|
| `archived = true` | `seen: true, read: true, archivedAt: now` |
| `read = true` | `seen: true, lastReadDate: now` |
| `read = false` | `seen: true, archived: false` |
| `seen = true` | `lastSeenDate: now` |
| `seen = false` | `read: false, archived: false, firstSeenDate: null` |
| `snoozedUntil` | `seen: true, archived: false` |

#### 3.3 计数查询方法

##### `getCount` (第441行)
按条件查询通知数量，支持分页指示（超过100条返回 hasMore 标记）。

##### `getCountBySeverity` (第472行)
按严重程度分组统计未读数量。

---

### 4. WebSocket 服务层 (`apps/ws`)

#### 4.1 网关层 (`socket/ws.gateway.ts:1-379`)

**核心职责：**
- 管理 WebSocket 连接生命周期
- 处理连接认证（JWT 校验）
- 按订阅者分组（Socket.IO rooms）
- 向在线客户端推送实时事件

**连接建立流程：**
```
客户端连接
    ↓
extractToken() - 从 handshake 提取 token
    ↓
getSubscriber() - JWT 校验，确认身份
    ↓
connection.join(subscriber._id) - 加入订阅者房间
    ↓
subscriberOnlineService.handleConnection() - 更新在线状态
```

**上下文隔离机制：**
每个连接携带 `contextKeys`，广播时进行精确匹配，确保多上下文场景下的数据隔离。

```typescript
private isExactMatch(messageContextKeys: string[], inboxContextKeys: string[]): boolean {
  if (messageContextKeys.length === 0) return inboxContextKeys.length === 0;
  if (messageContextKeys.length !== inboxContextKeys.length) return false;
  return messageContextKeys.every(key => inboxContextKeys.includes(key));
}
```

#### 4.2 事件路由 (`usecases/external-services-route/external-services-route.usecase.ts:1-157`)

根据事件类型执行不同的广播逻辑：

| 事件类型 | 处理逻辑 |
|----------|----------|
| `RECEIVED` | 推送完整通知内容 + 同步更新 unseen/unread 计数 |
| `UNSEEN` | 重新查询数据库，推送最新未见计数 |
| `UNREAD` | 重新查询数据库，推送最新未读计数（含严重程度分组） |

#### 4.3 在线状态管理 (`shared/subscriber-online/subscriber-online.service.ts:1-39`)

维护订阅者的在线状态：
```typescript
async handleConnection(subscriber) {
  await this.updateOnlineStatus(subscriber, { isOnline: true });
}

async handleDisconnection(subscriber, activeConnections) {
  // 只有当所有连接都断开时才标记为离线
  const isOnline = activeConnections > 1;
  await this.updateOnlineStatus(subscriber, { 
    isOnline, 
    lastOnlineAt: new Date().toISOString() 
  });
}
```

---

### 5. 队列与异步层 (`libs/application-generic`)

#### 5.1 WebSocket 队列服务 (`services/queues/web-sockets-queue.service.ts`)

**核心职责：**
- 将 WebSocket 广播任务异步化
- 支持 BullMQ（内存）和 SQS（生产）两种后端
- 按组织 ID 分组，避免同一用户消息乱序

**任务入队：**
```typescript
this.webSocketsQueueService.add({
  name: 'sendMessage',
  data: {
    event: WebSocketEventEnum.UNSEEN,
    userId: subscriber._id,
    _environmentId,
    contextKeys,
  },
  groupId: subscriber._organizationId,  // 按组织分组保证顺序
});
```

#### 5.2 WebSocket Worker (`apps/ws/src/socket/services/web-socket.worker.ts:1-108`)

消费队列中的广播任务：

```typescript
private getWorkerProcessor() {
  return async (job) => {
    const data: IWebSocketDataDto = job.data;
    
    await this.externalServicesRoute.execute(
      ExternalServicesRouteCommand.create({
        userId: data.userId,
        event: data.event,
        payload: data.payload,
        _environmentId: data._environmentId,
        contextKeys: data.contextKeys,
      })
    );
  };
}
```

**重要设计决策：**
> WS 任务采用 **at-most-once** 语义，失败不重试。因为 WS 消息是实时 UI 提示，过期后重播反而有害。客户端可通过主动拉取来修复状态。

#### 5.3 缓存失效服务 (`services/cache/invalidate-cache.service.ts`)

状态变更后立即失效相关缓存键：
```typescript
await this.invalidateCache.invalidateQuery({
  key: buildMessageCountKey().invalidate({
    subscriberId: subscriberId,
    _environmentId: environmentId,
  }),
});
```

#### 5.4 Webhook 发送 (`webhooks/usecases/send-webhook-message/send-webhook-message.usecase.ts`)

状态变更后触发外部系统通知，支持的事件类型：
- `message.seen` - 消息已见
- `message.read` - 消息已读
- `message.unread` - 消息标记未读
- `message.archived` - 消息已归档
- `message.unarchived` - 消息取消归档
- `message.snoozed` - 消息暂缓
- `message.unsnoozed` - 消息取消暂缓

---

### 6. 共享类型层 (`packages/shared`)

#### 6.1 WebSocket 事件枚举 (`types/ws.ts`)
```typescript
enum WebSocketEventEnum {
  RECEIVED = 'notification_received',
  UNREAD = 'unread_count_changed',
  UNSEEN = 'unseen_count_changed',
}
```

#### 6.2 消息状态枚举 (`types/messages.ts`)
```typescript
enum MessagesStatusEnum {
  READ = 'read',
  SEEN = 'seen',
  UNREAD = 'unread',
  UNSEEN = 'unseen',
}
```

---

## 三、完整链路时序图

### 场景1：用户标记通知为已读

```
[客户端A]                          [API服务]                    [数据库]                  [队列]                    [WebSocket服务]                [客户端B]
    |                                  |                            |                        |                           |                              |
    | 1. 用户点击"已读"                |                            |                        |                           |                              |
    |───────────────────────────┐     |                            |                        |                           |                              |
    |                           |     |                            |                        |                           |                              |
    | 2. 乐观更新本地 UI        |     |                            |                        |                           |                              |
    | emit('read.pending')     |     |                            |                        |                           |                              |
    |<──────────────────────────┘     |                            |                        |                           |                              |
    |                                  |                            |                        |                           |                              |
    | 3. PATCH /inbox/notifications/:id/read |                     |                        |                           |                              |
    │─────────────────────────────────>│                            |                        |                           │                              │
    |                                  | 4. 调用 MarkNotificationAs UseCase                    |                           │                              │
    |                                  │───────────────────────────┐|                        |                           │                              │
    |                                  |                           ||                        |                           │                              │
    |                                  | 5. updateMessagesStatusByIds()                       |                           │                              │
    |                                  │──────────────────────────────────────────────────────>│                        │                           │                              │
    |                                  |                            | 6. 更新 seen=true, read=true |                        |                           │                              │
    |                                  |                            │<───────────────────────|                        |                           │                              │
    |                                  | 7. invalidateCache()       |                        |                           │                              │
    |                                  │───────────────────────────────────┐                 |                           │                              │
    |                                  |                           │       |                 |                           │                              │
    |                                  | 8. webSocketsQueueService.add()  |                 |                           │                              │
    |                                  │────────────────────────────────────────────────────────────────────────────────>│                           │                              │
    |                                  |                            |                        | 9. 队列任务                 |                              │
    |                                  |                            |                        │───────────────────────────────────────────────────────────>│                              │
    |                                  |                            |                        |                           | 10. sendUnreadCountChange()  |                              │
    |                                  |                            |                        |                           │───────────────────────────────┐|                              |
    |                                  |                            |                        |                           |                              ||                              |
    |                                  |                            |                        |                           | 11. messageRepository.getCount()|                              |
    |                                  |                            |                        |                           │<──────────────────────────────|──────────────────────────────|
    |                                  |                            |                        |                           |                              |                               |
    |                                  |                            |                        |                           | 12. emit('unread_count_changed')|                              |
    |                                  |                            |                        |                           │───────────────────────────────────────────────────────────────────────────────────────>|
    |                                  |                            |                        |                           |                              |                               |
    |                                  |                            |                        |                           |                              | 13. 更新未读计数 UI            |
    |                                  |                            |                        |                           |                              │<──────────────────────────────|
    |                                  |                            |                        |                           |                              |                               |
    | 14. 返回更新后的通知             |                            |                        |                           |                              |                               |
    │<─────────────────────────────────│                            |                        |                           │                              │
    |                                  |                            |                        |                           │                              │
    | 15. emit('read.resolved')         |                            |                        |                           │                              │
    |<──────────────────────────────────|                            |                        |                           │                              │
```

### 场景2：断线重连后的状态补齐

```
[客户端]                          [WebSocket服务]                 [API服务]
    |                                     |                            |
    | 1. 网络断开                         |                            |
    │────────────断开────────────────────>│                            |
    |                                     |                            |
    | 2. socket.io 自动重连（指数退避）   |                            |
    │<───────────────────────────────────┤                            |
    |                                     |                            |
    | 3. connect 事件触发                 |                            |
    | emit('socket.connect.resolved')     |                            |
    |<───────────────────────────┐        |                            |
    |                            |        |                            |
    | 4. 应用层主动拉取最新状态  |        |                            |
    │ GET /inbox/notifications/count    |                            |
    │───────────────────────────────────────────────────────────────>│
    |                                     |                            |
    │ GET /inbox/notifications?limit=10 |                            |
    │───────────────────────────────────────────────────────────────>│
    |                                     |                            |
    | 5. 接收最新数据，刷新本地缓存       |                            |
    │<───────────────────────────────────────────────────────────────│
    |                                     |                            |
    | 6. WebSocket 推送实时计数           |                            |
    │<────────────────────────────────────┤                            |
    |      (unseen_count_changed)         |                            |
    │<────────────────────────────────────┤                            |
    |      (unread_count_changed)         |                            |
```

---

## 四、关键设计决策与技术要点

### 1. 状态模型设计

**Seen vs Read 的区别：**
- **Seen（已见）**：通知出现在用户视野中，自动标记
- **Read（已读）**：用户主动点击查看，需要显式操作

**状态联动规则：**
- 标记为已读 → 自动标记为已见
- 标记为已归档 → 自动标记为已读+已见
- 标记为未见 → 自动取消已读+已归档

### 2. 多终端同步策略

**最终一致性保障：**
1. **实时通道**：WebSocket 推送计数变更事件（秒级）
2. **数据库基准**：HTTP API 拉取确保数据准确（用户主动刷新时）
3. **缓存失效**：状态变更后立即失效服务端缓存
4. **幂等操作**：重复标记同一状态不会产生副作用

### 3. 断线重连补齐机制

系统 **没有** 采用"事件溯源 + 离线消息队列"的复杂方案，而是采用更简洁的最终一致性策略：

1. **WebSocket 自动重连**：socket.io 内置重连机制，指数退避
2. **重连后主动拉取**：应用层监听 `connect` 事件，触发全量数据刷新
3. **计数基准对齐**：每次 WebSocket 推送的是数据库实时查询结果，不是增量事件
4. **本地缓存自愈**：客户端缓存监听所有状态事件，自动合并更新

**设计考量：**
- 避免维护复杂的离线消息队列和 ack 机制
- 计数推送是幂等的，重复推送不影响正确性
- 用户感知不到短暂的不一致（秒级内自愈）

### 4. Context Keys 隔离机制

支持同一订阅者在不同上下文（如不同租户、不同项目）下的状态隔离：
- 连接时携带 `contextKeys`
- 状态更新时指定 `contextKeys` 筛选条件
- WebSocket 广播时进行精确匹配
- 数据库查询时加入 `contextKeys` 过滤

### 5. 性能优化点

| 优化手段 | 实现位置 | 效果 |
|---------|----------|------|
| 批量更新分批处理 | `message.repository.ts` | 避免单次更新过多文档 |
| 乐观更新 | `packages/js/src/notifications/helpers.ts` | UI 无延迟响应 |
| 本地缓存 | `notifications-cache.ts` | 减少重复网络请求 |
| 服务端缓存 | `invalidate-cache.service.ts` | 加速计数查询 |
| 异步广播 | `web-sockets-queue.service.ts` | 解耦 HTTP 请求与 WS 推送 |
| 上下文匹配 | `ws.gateway.ts` | 避免不必要的消息发送 |
| 计数分页指示 | `getCount()` | 超过100条时返回 hasMore，避免全表扫描 |

---

## 五、核心文件索引

| 层级 | 文件路径 | 核心内容 |
|------|----------|----------|
| **客户端 SDK** | `packages/js/src/novu.ts` | SDK 入口，模块编排 |
| | `packages/js/src/ws/socket.ts` | WebSocket 连接管理 |
| | `packages/js/src/notifications/helpers.ts` | 状态操作与乐观更新 |
| | `packages/js/src/api/inbox-service.ts` | HTTP API 封装 |
| | `packages/js/src/cache/notifications-cache.ts` | 本地缓存管理 |
| **API 服务** | `apps/api/src/app/inbox/inbox.controller.ts` | HTTP 路由定义 |
| | `apps/api/src/app/inbox/usecases/mark-notifications-as-seen/` | 标记已见业务逻辑 |
| | `apps/api/src/app/inbox/usecases/mark-many-notifications-as/` | 批量更新状态 |
| | `apps/api/src/app/inbox/usecases/update-all-notifications/` | 按条件全量更新 |
| **WebSocket 服务** | `apps/ws/src/socket/ws.gateway.ts` | WebSocket 网关，连接管理 |
| | `apps/ws/src/socket/usecases/external-services-route/` | 事件路由与广播 |
| | `apps/ws/src/socket/services/web-socket.worker.ts` | 队列消费处理器 |
| | `apps/ws/src/shared/subscriber-online/` | 在线状态管理 |
| **数据访问** | `libs/dal/src/repositories/message/message.repository.ts` | 消息 CRUD，状态更新 |
| | `libs/dal/src/repositories/message/message.entity.ts` | 消息数据模型 |
| **共享层** | `packages/shared/src/types/ws.ts` | WebSocket 事件枚举 |
| | `packages/shared/src/types/messages.ts` | 消息状态枚举 |
| | `packages/shared/src/webhooks/webhook-event.enum.ts` | Webhook 事件类型 |
| **通用服务** | `libs/application-generic/src/services/queues/web-sockets-queue.service.ts` | WS 队列服务 |
| | `libs/application-generic/src/services/cache/` | 缓存管理与失效 |
| | `libs/application-generic/src/webhooks/` | Webhook 发送 |

---

## 六、总结

### 架构优点
1. **职责清晰**：各层边界明确，单一职责原则贯彻良好
2. **高内聚低耦合**：通过事件总线和队列解耦各模块
3. **用户体验优先**：乐观更新确保 UI 响应迅速
4. **可扩展性**：Use Case 模式便于新增操作类型
5. **可靠性**：数据库持久化 + 最终一致性确保数据不丢

### 关键设计模式
- **命令模式**：Use Case + Command 封装业务操作
- **观察者模式**：Event Emitter 实现模块间通信
- **仓库模式**：Repository 封装数据访问
- **乐观并发**：客户端乐观更新 + 服务端最终覆盖
- **异步队列**：解耦同步请求与异步广播

---

## 七、断线恢复机制深度解析

### 7.1 `socket.connect.resolved` 事件触发链路

#### 7.1.1 事件发射源

**事件由两个 Socket 实现类各自独立发射：**

**1. Socket.IO 客户端 (`packages/js/src/ws/socket.ts:191-197`)**
```typescript
// 代码证据: socket.ts 第180-196行
this.#socketIo = io(this.#socketUrl, {
  reconnectionDelayMax: 10000,  // socket.io 内置自动重连
  transports: ['websocket'],
  query: { token: `${this.#token}` },
  ...(this.#socketOptions ?? {}),
});

this.#socketIo.on('connect', () => {
  this.#emitter.emit('socket.connect.resolved', { args });  // 连接成功
});

this.#socketIo.on('connect_error', (error) => {
  this.#emitter.emit('socket.connect.resolved', { args, error });  // 连接失败
});
```
> **证据**：`packages/js/src/ws/socket.ts:180-196`
> - Socket.IO 客户端依赖 `socket.io-client` 库内置的自动重连机制
> - 重连延迟策略：指数退避，最大延迟 10000ms
> - 无论是首次连接还是重连成功，都会触发 `socket.connect.resolved` 事件

**2. PartySocket 客户端 (`packages/js/src/ws/party-socket.ts:268-275`)**
```typescript
// 代码证据: party-socket.ts 第259-274行
const args = { socketUrl: this.#socketUrl };
this.#emitter.emit('socket.connect.pending', { args });

// ...

socket.addEventListener('open', () => {
  this.#startHibernationHeartbeat();
  this.#emitter.emit('socket.connect.resolved', { args });  // 连接成功
});

socket.addEventListener('error', (error) => {
  this.#emitter.emit('socket.connect.resolved', { args, error });  // 连接失败
});
```
> **证据**：`packages/js/src/ws/party-socket.ts:259-274`
> - PartySocket 基于 `partysocket` 库（Cloudflare WebSocket 客户端）
> - 同样支持自动重连，但事件监听方式为标准 `addEventListener`
> - 额外包含休眠心跳机制（每 25s 发送 ping）

#### 7.1.2 事件监听者与重新拉取触发

**关键发现：SDK 内部 **没有** 自动监听 `socket.connect.resolved` 事件并触发重新拉取！**

**SDK 内部监听的事件列表（证据来源 `notifications-cache.ts:88-111`）：**
```typescript
// 代码证据: notifications-cache.ts 第88-111行
const updateEvents: NotificationEvents[] = [
  'notification.read.pending',
  'notification.read.resolved',
  'notification.unread.pending',
  'notification.unread.resolved',
  'notification.complete_action.pending',
  'notification.complete_action.resolved',
  'notification.revert_action.pending',
  'notification.revert_action.resolved',
  'notifications.read_all.pending',
  'notifications.read_all.resolved',
];

const removeEvents: NotificationEvents[] = [
  'notification.archive.pending',
  'notification.unarchive.pending',
  'notification.snooze.pending',
  'notification.unsnooze.pending',
  'notification.delete.pending',
  'notifications.archive_all.pending',
  'notifications.archive_all_read.pending',
  'notifications.delete_all.pending',
];
```
> **证据**：`packages/js/src/cache/notifications-cache.ts:88-111`
> - 缓存只监听通知状态变更事件，不监听 socket 连接事件
> - **`socket.connect.resolved` 不在监听列表中**

**全局搜索验证：**
在 `packages/js/src/` 目录下搜索 `socket.connect.resolved` 监听器，结果为 **0 匹配**。
> **证据**：Grep 搜索结果 `glob:**/packages/js/src/**/*.ts`，pattern:`socket\.connect\.resolved`，无监听者匹配

#### 7.1.3 责任归属结论

| 机制 | SDK 内建功能 | 接入方责任 |
|------|-------------|-----------|
| WebSocket 自动重连 | ✅ 是（socket.io/partysocket 内置） | ❌ |
| 发射 `socket.connect.resolved` 事件 | ✅ 是 | ❌ |
| 监听连接事件并触发重新拉取 | ❌ **否** | ✅ **是** |
| WebSocket 实时计数推送接收 | ✅ 是 | ❌ |
| 本地缓存与事件同步 | ✅ 是 | ❌ |

**接入方实现示例（React/Solid Hooks）：**
```typescript
// 接入方需要自行实现重连后的刷新逻辑
const novu = useNovu();

useEffect(() => {
  const cleanup = novu.on('socket.connect.resolved', ({ error }) => {
    if (!error) {
      // 重新拉取通知列表和计数
      novu.notifications.list();
      novu.notifications.count();
    }
  });
  
  return cleanup;
}, [novu]);
```

---

### 7.2 `socket.ts` vs `party-socket.ts` 行为差异详解

#### 7.2.1 触发条件差异（何时使用哪个实现）

**选择逻辑 (`packages/js/src/ws/socket-factory.ts:37-76`)**
```typescript
// 代码证据: socket-factory.ts 第9-43行
const PARTY_SOCKET_URLS = [
  'wss://eu.socket.novu.co',
  'wss://socket.novu.co',           // 生产环境默认
  'wss://socket.novu-staging.co',
];

const URL_TRANSFORMATIONS: Record<string, string> = {
  'https://eu.ws.novu.co': 'wss://eu.socket.novu.co',
  'https://ws.novu.co': 'wss://socket.novu.co',
  'https://dev.ws.novu.co': 'wss://socket.novu-staging.co',
};

function resolveSocketType(socketUrl?: string, explicitType?: SocketTypeOption): SocketType {
  if (explicitType) {
    return SOCKET_TYPE_OPTION_MAP[explicitType];
  }
  // 云环境 URL 自动使用 PartySocket，自托管默认使用 Socket.IO
  return shouldUsePartySocket(socketUrl) ? SocketType.PARTY_SOCKET : SocketType.SOCKET_IO;
}
```
> **证据**：`packages/js/src/ws/socket-factory.ts:9-43`

| 部署类型 | 默认 Socket 实现 | 触发条件 |
|---------|----------------|----------|
| **Novu Cloud** | PartySocket | URL 匹配 `PARTY_SOCKET_URLS` 列表 |
| **自托管 (Self-hosted)** | Socket.IO | URL 不在列表中，或显式指定 `socketType: 'self-hosted'` |

#### 7.2.2 行为差异对比表

| 特性 | `socket.ts` (Socket.IO) | `party-socket.ts` (PartySocket) |
|------|------------------------|--------------------------------|
| **底层库** | `socket.io-client` | `partysocket` (Cloudflare) |
| **自动重连** | ✅ 内置（指数退避，最大10s） | ✅ 内置 |
| **心跳机制** | ❌ 依赖协议层 ping | ✅ **应用层心跳**（每25s发送ping） |
| **休眠检测** | ❌ | ✅ `HIBERNATION_HEARTBEAT_MS = 25000` |
| **认证方式** | Query 参数 `?token=xxx` | Query 参数 `?token=xxx` |
| **事件发射** | `socket.connect.resolved` | `socket.connect.resolved` |
| **消息协议** | Socket.IO 二进制协议 | 标准 WebSocket + JSON |
| **生产URL** | `https://ws.novu.co` | `wss://socket.novu.co` |
| **连接关闭清理** | 简单 disconnect | ✅ 清理心跳定时器 |

**PartySocket 休眠心跳实现 (`party-socket.ts:228-251`)：**
```typescript
// 代码证据: party-socket.ts 第228-251行
#startHibernationHeartbeat(): void {
  this.#clearHibernationHeartbeat();
  this.#hibernationHeartbeatIntervalId = setInterval(() => {
    const socket = this.#partySocket;
    if (!socket || socket.readyState !== WebSocket.OPEN) {
      return;
    }
    try {
      socket.send(HIBERNATION_PING_PAYLOAD);  // 'ping'
    } catch {
      // Socket may have closed between readyState check and send
    }
  }, HIBERNATION_HEARTBEAT_MS);  // 25,000ms
}
```
> **证据**：`packages/js/src/ws/party-socket.ts:228-251`
> - 设计目的：防止移动设备/浏览器休眠导致 WebSocket 连接被回收
> - 每 25 秒发送一次 ping 消息保持连接活跃

---

## 八、完整端到端调用链（标注代码证据）

### 8.1 状态上报 → 服务端持久化链路

**链路：客户端标记已读 → HTTP API → UseCase → 数据库更新**

```
[客户端 SDK]
    │
    ▼ 1. helpers.ts: read() 函数触发
    │  证据: packages/js/src/notifications/helpers.ts
    │  - 乐观更新本地缓存
    │  - emit('notification.read.pending')
    │
    ▼ 2. API 调用 inboxService.read()
    │  证据: packages/js/src/api/inbox-service.ts
    │  - PATCH /inbox/notifications/:id/read
    │
───────────────────────────────────────────────────── HTTP 请求
    │
[API 服务]
    │
    ▼ 3. inbox.controller.ts: markNotificationAsRead()
    │  证据: apps/api/src/app/inbox/inbox.controller.ts:~157
    │  - AuthGuard('subscriberJwt') 鉴权
    │  - 构建 MarkNotificationAsCommand
    │
    ▼ 4. MarkNotificationAs UseCase 执行
    │  证据: apps/api/src/app/inbox/usecases/mark-notification-as/mark-notification-as.usecase.ts:22-75
    │  - 校验订阅者存在
    │  - 查找原消息
    │  - 调用 MarkManyNotificationsAs
    │
    ▼ 5. MarkManyNotificationsAs UseCase 执行
    │  证据: apps/api/src/app/inbox/usecases/mark-many-notifications-as/mark-many-notifications-as.usecase.ts:38-111
    │
    ▼ 6. 数据库更新
    │  证据: libs/dal/src/repositories/message/message.repository.ts:751
    │  - updateMessagesStatusByIds()
    │  - 状态联动逻辑（read=true 自动设置 seen=true）
    │
    ▼ 7. 缓存失效
    │  证据: mark-many-notifications-as.usecase.ts:64-69
    │  - invalidateCacheService.invalidateQuery()
    │  - key: buildMessageCountKey()
    │
    ▼ 8. WebSocket 队列任务入队
    │  证据: mark-many-notifications-as.usecase.ts:101-110
    │  - webSocketsQueueService.add()
    │  - event: WebSocketEventEnum.UNREAD
    │
    ▼ 9. Webhook 发送（批量）
    │  证据: mark-many-notifications-as.usecase.ts:99, 113-131
    │  - 每批 100 条
    │  - 事件类型: MESSAGE_READ / MESSAGE_UNREAD 等
```

---

### 8.2 队列广播 → 其他终端接收链路

**链路：队列任务 → Worker 消费 → WebSocket 网关 → 多终端推送**

```
[队列层]
    │
    ▼ 1. WebSocketsQueueService.add() 入队
    │  证据: libs/application-generic/src/services/queues/web-sockets-queue.service.ts:40-69
    │  - 先检查是否启用新 Socket Worker
    │  - 如启用直接调用 socketWorkerService.sendMessage()
    │  - 否则走 BullMQ/SQS 队列
    │
    ▼ 2. WebSocket Worker 消费
    │  证据: apps/ws/src/socket/services/web-socket.worker.ts
    │  - getWorkerProcessor() 处理队列任务
    │  - at-most-once 语义，失败不重试
    │
    ▼ 3. ExternalServicesRoute UseCase 路由
    │  证据: apps/ws/src/socket/usecases/external-services-route/external-services-route.usecase.ts:17-37
    │  - 先检查用户是否在线（connectionExist）
    │  - 不在线直接返回，避免无效计算
    │
    ▼ 4. 根据事件类型处理
    │  ├─ RECEIVED 事件: 推送完整消息 + 同步计数
    │  ├─ UNSEEN 事件: sendUnseenCountChange()
    │  └─ UNREAD 事件: sendUnreadCountChange()
    │     证据: external-services-route.usecase.ts:61-100
    │     - 重新查询数据库获取最新计数
    │     - messageRepository.getCount() + getCountBySeverity()
    │     - 超过100条返回 hasMore 标记
    │
    ▼ 5. WSGateway.sendMessage() 广播
    │  证据: apps/ws/src/socket/ws.gateway.ts:170-200
    │  - 获取用户房间内的所有 socket
    │  - contextKeys 精确匹配校验
    │  - 匹配成功才发送事件
    │
───────────────────────────────────────────────────── WebSocket 推送
    │
[其他终端]
    │
    ▼ 6. 客户端 Socket 接收
    │  证据: packages/js/src/ws/socket.ts:168-172 (Socket.IO)
    │  证据: packages/js/src/ws/party-socket.ts:189-200 (PartySocket)
    │  - 解析 JSON 消息
    │  - 匹配 event 类型
    │
    ▼ 7. Emitter 发射事件
    │  证据: socket.ts:162-171, party-socket.ts:176-199
    │  - 'notifications.unread_count_changed'
    │  - 'notifications.unseen_count_changed'
    │
    ▼ 8. 本地缓存更新 + UI 刷新
    │  证据: packages/js/src/cache/notifications-cache.ts
    │  - 缓存监听状态事件自动更新
    │  - UI Hooks 监听事件刷新视图
```

---

### 8.3 断线后状态补齐的实际调用链

**链路：网络断开 → 自动重连 → 接入方主动拉取 → 状态自愈**

```
[客户端]
    │
    ▼ 1. 网络断开检测
    │  Socket.IO: 内置心跳超时检测
    │  PartySocket: 连接关闭事件
    │
    ▼ 2. 自动重连启动（指数退避）
    │  证据: socket.ts:183 reconnectionDelayMax: 10000
    │
    ▼ 3. 重连成功 → emit('socket.connect.resolved')
    │  证据: socket.ts:191-193, party-socket.ts:268-270
    │
    ▼ 4. ⚠️ SDK 内部不自动拉取 ⚠️
    │  （需要接入方监听事件并主动调用）
    │
    ▼ 5. 接入方主动调用
    │  ├─ novu.notifications.list()  // 拉取列表
    │  └─ novu.notifications.count() // 拉取计数
    │
───────────────────────────────────────────────────── HTTP 请求
    │
[API 服务]
    │
    ▼ 6. GetNotifications UseCase
    │  证据: apps/api/src/app/inbox/usecases/get-notifications/
    │  - 查询最新通知列表
    │
    ▼ 7. NotificationsCount UseCase
    │  证据: apps/api/src/app/inbox/usecases/notifications-count/
    │  - 查询最新未读/未见计数
    │
───────────────────────────────────────────────────── HTTP 响应
    │
[客户端]
    │
    ▼ 8. 更新本地缓存 + UI 刷新
    │  证据: notifications-cache.ts
    │  - 缓存存储最新数据
    │  - 事件通知 UI 组件
    │
    ▼ 9. WebSocket 实时推送恢复
    │  - 后续状态变更通过 WebSocket 实时同步
```

---

## 九、关键代码证据索引

| 分析点 | 文件路径 | 行号 |
|--------|----------|------|
| **Socket.IO 连接事件发射** | `packages/js/src/ws/socket.ts` | 180-196 |
| **PartySocket 连接事件发射** | `packages/js/src/ws/party-socket.ts` | 259-274 |
| **PartySocket 休眠心跳** | `packages/js/src/ws/party-socket.ts` | 228-251 |
| **Socket 工厂选择逻辑** | `packages/js/src/ws/socket-factory.ts` | 9-43, 55-76 |
| **缓存监听事件列表** | `packages/js/src/cache/notifications-cache.ts` | 88-111 |
| **全局 socket.connect.resolved 监听搜索** | Grep 搜索 | 0 匹配 |
| **MarkNotificationAs UseCase** | `apps/api/src/app/inbox/usecases/mark-notification-as/` | 22-75 |
| **MarkManyNotificationsAs UseCase** | `apps/api/src/app/inbox/usecases/mark-many-notifications-as/` | 38-111 |
| **数据库状态更新** | `libs/dal/src/repositories/message/message.repository.ts` | 751, 790, 861 |
| **WS 队列服务** | `libs/application-generic/src/services/queues/web-sockets-queue.service.ts` | 40-69 |
| **WS Worker 处理器** | `apps/ws/src/socket/services/web-socket.worker.ts` | 全程 |
| **外部服务路由** | `apps/ws/src/socket/usecases/external-services-route/` | 17-37, 61-100 |
| **WS 网关消息发送** | `apps/ws/src/socket/ws.gateway.ts` | 170-200 |
| **上下文匹配逻辑** | `apps/ws/src/socket/ws.gateway.ts` | ~200+ |
| **在线状态管理** | `apps/ws/src/shared/subscriber-online/` | 全程 |
| **Novu 入口类** | `packages/js/src/novu.ts` | 109-123 |

---

## 十、修正与澄清（基于代码证据）

### 10.1 关于断线重连的澄清

**之前的文档描述不准确，基于代码证据修正：**

| 之前描述 | 实际代码行为 |
|---------|-------------|
| "应用层监听 `connect` 事件，触发全量数据刷新" | ❌ **SDK 内部不监听**，需要接入方自行实现 |
| "断线后 SDK 自动补齐缺失事件" | ❌ **没有自动补齐**，依赖：<br>1. 接入方主动拉取<br>2. WebSocket 实时推送作为基准对齐 |

### 10.2 关于两种 Socket 实现的澄清

**不是二选一的配置，而是基于部署环境自动选择：**
- Novu Cloud 环境 → 自动使用 PartySocket（带休眠心跳）
- 自托管环境 → 自动使用 Socket.IO（标准实现）
- 可通过 `socketOptions.socketType` 显式覆盖

### 10.3 关于状态同步最终一致性的澄清

**实际的最终一致性保障是三层机制：**
1. **HTTP API 基准拉取**（接入方负责，按需触发）
2. **WebSocket 计数推送**（服务端主动，每次重新查询数据库）
3. **本地缓存事件合并**（SDK 内建，监听状态变更事件）
