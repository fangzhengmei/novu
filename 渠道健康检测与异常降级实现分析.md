# 发送渠道健康检测与异常降级实现分析

## 一、系统整体架构概览

Novu 消息发送系统的健康检测与异常降级机制主要分布在以下几个核心模块：

| 模块 | 主要职责 | 关键文件 |
|------|----------|----------|
| 队列服务健康检测 | 监控消息队列运行状态 | `libs/application-generic/src/health/` |
| 发送任务重试机制 | 发送失败后的重试策略 | `apps/worker/src/app/workflow/services/standard.worker.ts` |
| 集成选择与降级 | 主/备集成切换逻辑 | `libs/application-generic/src/usecases/select-integration/` |
| Novu 内置 Provider | 降级到 Novu 托管服务 | `libs/application-generic/src/usecases/get-novu-provider-credentials/` |
| 集成健康检查 | 特定渠道（如 MS Teams）健康状态 | `apps/api/src/app/integrations/usecases/msteams-health-check/` |
| 组织级 Kill Switch | 全局流量控制开关 | `packages/shared/src/types/feature-flags.ts` |
| 队列后端降级 | SQS 到 BullMQ 的自动降级 | `libs/application-generic/src/services/queues/queue-base.service.ts` |

---

## 二、探测频率和采样方法

### 2.1 基础设施健康检测

#### 2.1.1 队列健康检测
**实现文件**：`libs/application-generic/src/health/queue-health-indicator.service.ts`

- **检测触发方式**：通过 `/health-check` 端点被动触发，由外部监控系统（如 Kubernetes liveness/readiness probes）轮询调用
- **检测频率**：由部署配置决定，通常为 10-30 秒一次
- **检测内容**：
  - Redis 连接状态 `isClientReady()`
  - 队列是否暂停 `isQueuePaused()`
  - Worker 是否运行 `isWorkerRunning()`

```typescript
// queue-health-indicator.service.ts:18-31
async isHealthy(): Promise<HealthIndicatorResult> {
  return await this.handleHealthCheck();
}

async handleHealthCheck() {
  const isReady = this.queueService.isReady();
  const result = this.getStatus(this.indicatorKey, isReady);
  if (isReady) {
    return result;
  }
  throw new HealthCheckError(`${this.serviceName} Health is not ready`, result);
}
```

#### 2.1.2 支持的健康检测指标

| 检测类型 | 实现文件 | 检测内容 |
|----------|----------|----------|
| DAL 数据库 | `dal.health-indicator.ts` | MongoDB 连接状态 |
| 缓存服务 | `cache.health-indicator.ts` | Redis/ElastiCache 连接 |
| 工作流队列 | `workflow-queue.health-indicator.ts` | 工作流处理队列状态 |
| 标准队列 | `standard-queue.health-indicator.ts` | 标准任务队列状态 |
| 订阅者队列 | `subscriber-process-queue.health-indicator.ts` | 订阅者处理队列 |
| WebSocket 队列 | `web-sockets-queue.health-indicator.ts` | WebSocket 推送队列 |
| 入站解析队列 | `inbound-parse-queue.health-indicator.ts` | 入站邮件解析队列 |

### 2.2 发送失败实时采样

**实现文件**：`apps/worker/src/app/workflow/services/standard.worker.ts:52-86`

- **采样方式**：100% 全量采样，每次发送失败都会触发事件
- **事件监听**：
  - `failed` 事件：任务失败时触发 `jobHasFailed()`
  - `completed` 事件：任务完成时触发 `jobHasCompleted()`

```typescript
// standard.worker.ts:52-58
this.bullMqWorker.on('failed', async (job: Job<IStandardDataDto, void, string>, error: Error): Promise<void> => {
  await this.jobHasFailed(job, error);
});

this.bullMqWorker.on('completed', async (job: Job<IStandardDataDto, void, string>): Promise<void> => {
  await this.jobHasCompleted(job);
});
```

### 2.3 MS Teams 集成健康检查

**实现文件**：`apps/api/src/app/integrations/usecases/msteams-health-check/msteams-health-check.usecase.ts`

- **触发方式**：Dashboard 前端轮询调用 `/integrations/:integrationId/msteams-health`
- **检测频率**：前端控制，通常在设置流程中每 2-5 秒轮询一次
- **检测维度**（4个检查点并行执行）：
  1. **appRegistration**：Graph API Token 获取能力
  2. **azureBotCreated**：Azure Bot 资源部署状态
  3. **teamsAppCatalog**：Teams 应用目录发布状态
  4. **permissions**：Graph API 权限配置

```typescript
// msteams-health-check.usecase.ts:95-100
const [appRegistration, azureBotCreated, teamsAppCatalog, permissions] = await Promise.all([
  shouldRun('appRegistration') ? this.checkAppRegistration(clientId, secretKey, tenantId) : Promise.resolve(null),
  shouldRun('azureBotCreated') ? Promise.resolve(this.checkAzureBotCreated(integration)) : Promise.resolve(null),
  shouldRun('teamsAppCatalog') ? this.checkTeamsAppCatalog(clientId, secretKey, tenantId) : Promise.resolve(null),
  shouldRun('permissions') ? this.checkPermissions(clientId, secretKey, tenantId) : Promise.resolve(null),
]);
```

### 2.4 执行详情记录

**实现文件**：`libs/application-generic/src/usecases/create-execution-details/create-execution-details.usecase.ts`

- **采样率**：100% 记录所有发送事件
- **记录内容**：
  - Provider 选择信息 `createProviderSelectedMessage()`
  - 消息创建 `MESSAGE_CREATED`
  - 发送成功 `MESSAGE_SENT`
  - 发送失败 `PROVIDER_ERROR`
  - 重试事件 `WEBHOOK_FILTER_FAILED_RETRY`

---

## 三、健康标记的消费方式

### 3.1 任务状态流转

**实现文件**：`apps/worker/src/app/workflow/usecases/update-job-status/`

#### 3.1.1 成功标记消费
- 触发时机：Worker `completed` 事件
- 状态变更：`RUNNING` → `COMPLETED`
- 副作用：触发下游依赖任务

```typescript
// standard.worker.ts:202-228
private async jobHasCompleted(job: Job<IStandardDataDto, void, string>): Promise<void> {
  await this.jobRepository.updateOne(
    { _environmentId: minimalData.environmentId, _id: minimalData.jobId, status: JobStatusEnum.RUNNING },
    { $set: { status: JobStatusEnum.COMPLETED } }
  );
}
```

#### 3.1.2 失败标记消费
- 触发时机：Worker `failed` 事件
- 判断逻辑：
  1. `shouldBackoff(error)`：是否需要重试
  2. `hasReachedMaxAttempts`：是否达到最大重试次数（默认 3 次）
- 处理分支：
  - 需要重试且未达上限 → 返回 `true` 触发 BullMQ 重试
  - 需要重试但已达上限 → 执行 `handleLastFailedJob()` 终结处理
  - 不需要重试 → 直接标记为失败

```typescript
// standard.worker.ts:230-285
private async jobHasFailed(job: Job<IStandardDataDto, void, string>, error: Error): Promise<boolean> {
  const hasToBackoff = this.runJob.shouldBackoff(error);
  const hasReachedMaxAttempts = job.attemptsMade >= this.DEFAULT_ATTEMPTS;
  const shouldHandleLastFailedJob = hasToBackoff && hasReachedMaxAttempts;

  const shouldBeSetAsFailed = !hasToBackoff || shouldHandleLastFailedJob;
  if (shouldBeSetAsFailed) {
    await this.setJobAsFailed.execute(SetJobAsFailedCommand.create({ ...minimalData, isLastJobFailed: isLastJobInWorkflow }), error);
  }

  if (shouldHandleLastFailedJob) {
    await this.handleLastFailedJob.execute(HandleLastFailedJobCommand.create({ ...minimalData, error }));
  }

  return hasToBackoff && !hasReachedMaxAttempts;
}
```

### 3.2 执行详情消费链

执行详情通过 `CreateExecutionDetails` 用例写入数据库后，主要用于：

1. **活动日志展示**：Dashboard 活动日志页展示发送历史
2. **Webhook 通知**：触发外部系统 Webhook 事件
3. **问题排查**：运营人员查看单条消息发送详情
4. **统计分析**：ClickHouse 批量分析发送成功率

---

## 四、自动降级和回切策略

### 4.1 队列后端降级（SQS → BullMQ）

**实现文件**：`libs/application-generic/src/services/queues/queue-base.service.ts:186-264`

这是 Novu 最核心的降级机制，支持四种运行模式：

| 模式 | 行为 |
|------|------|
| `BULLMQ` | 仅使用 BullMQ |
| `SHADOW` | BullMQ 主 + SQS 影子 |
| `LIVE` | SQS 主 + BullMQ 影子 |
| `COMPLETE` | 仅 SQS，失败自动降级到 BullMQ |

#### 4.1.1 降级触发条件
- SQS `sendMessage` / `sendMessageBatch` API 调用抛出异常
- 组织级 Feature Flag 控制 `QUEUE_BACKEND_MODE`

#### 4.1.2 降级流程（COMPLETE 模式）
```typescript
// queue-base.service.ts:242-259
case QueueBackendMode.COMPLETE: {
  try {
    return await this.addJobsToSQS(jobs, organizationId);
  } catch (error) {
    // SQS 失败，自动降级到 BullMQ
    Logger.error({ topic: this.topic, error }, 'SQS failed in COMPLETE mode, falling back to BullMQ', LOG_CONTEXT);
    return await this.addJobsToBullMQ(jobs);
  }
}
```

#### 4.1.3 降级流程（LIVE 模式）
```typescript
// queue-base.service.ts:208-240
case QueueBackendMode.LIVE: {
  try {
    await this.addJobsToSQS(jobs, organizationId);
    try {
      await this.addJobsToBullMQ(this.markAsSkipProcessing(jobs));
    } catch (bullmqError) {
      // BullMQ 影子失败不影响主流程
      Logger.warn({ error: bullmqError }, 'BullMQ fallback failed in LIVE mode');
    }
  } catch (error) {
    Logger.error({ error }, 'SQS failed in LIVE mode, falling back to BullMQ as primary', LOG_CONTEXT);
    await this.addJobsToBullMQ(jobs);
  }
  break;
}
```

#### 4.1.4 回切策略
- **自动回切**：无内置自动回切，依赖 Feature Flag 变更
- **手动回切**：通过 LaunchDarkly 修改 `QUEUE_BACKEND_MODE` 标志位

### 4.2 发送重试退避策略

**实现文件**：`apps/worker/src/app/workflow/usecases/webhook-filter-backoff-strategy/webhook-filter-backoff-strategy.usecase.ts`

#### 4.2.1 指数退避算法
```typescript
// webhook-filter-backoff-strategy.usecase.ts:35
return Math.round(Math.random() * 2 ** attemptsMade * 1000);
```

| 重试次数 | 最小延迟 | 最大延迟 |
|---------|---------|---------|
| 第 1 次 | 0 ms | 2000 ms |
| 第 2 次 | 0 ms | 4000 ms |
| 第 3 次 | 0 ms | 8000 ms |

**特性**：
- 最大重试次数：`DEFAULT_ATTEMPTS = 3` 次
- 随机性：加入 `Math.random()` 避免惊群效应
- 退避类型：二进制指数退避（Exponential Backoff）

### 4.3 Novu 内置 Provider 降级

**实现文件**：`libs/application-generic/src/usecases/get-novu-provider-credentials/get-novu-provider-credentials.usecase.ts`

#### 4.3.1 降级触发
当用户没有配置自定义 Provider 时，自动降级到 Novu 托管的 Provider：

| 渠道 | Novu Provider ID | 环境变量配置 |
|------|-----------------|-------------|
| 邮件 | `novu-email` | `NOVU_EMAIL_INTEGRATION_API_KEY` |
| 短信 | `novu-sms` | `NOVU_SMS_INTEGRATION_ACCOUNT_SID`, `NOVU_SMS_INTEGRATION_TOKEN` |
| 聊天 | `novu-chat` | `NOVU_SLACK_INTEGRATION_CLIENT_ID` |

#### 4.3.2 使用限制
Novu 内置 Provider 有月度使用限额：

```typescript
// calculate-limit-novu-integration.usecase.ts:13-17
static MAX_NOVU_INTEGRATION_MAIL_REQUESTS = parseInt(process.env.MAX_NOVU_INTEGRATION_MAIL_REQUESTS || '300', 10);
static MAX_NOVU_INTEGRATION_SMS_REQUESTS = parseInt(process.env.MAX_NOVU_INTEGRATION_SMS_REQUESTS || '20', 10);
static MAX_NOVU_INTEGRATION_CHAT_REQUESTS = parseInt(process.env.MAX_NOVU_INTEGRATION_CHAT_REQUESTS || '300', 10);
```

| 渠道 | 默认限额 | 统计周期 |
|------|---------|---------|
| 邮件 | 300 条/月 | 自然月 |
| 短信 | 20 条/月 | 自然月 |
| 聊天 | 300 条/月 | 自然月 |

#### 4.3.3 限流触发
超过限额时抛出 `ConflictException`，发送任务失败：
```typescript
// get-novu-provider-credentials.usecase.ts:64-73
if (limit.count >= limit.limit) {
  throw new ConflictException(`Limit for Novu's ${integration.channelType.toLowerCase()} provider was reached.`);
}
```

### 4.4 集成条件路由降级

**实现文件**：`libs/application-generic/src/usecases/select-integration/select-integration.usecase.ts`

#### 4.4.1 集成选择优先级
1. **指定 identifier**：直接按标识符选择
2. **条件匹配集成**：遍历集成的 conditions 字段，第一个匹配的胜出
3. **主集成（Primary）**：Email/SMS 渠道支持标记 Primary
4. **Novu 内置集成**：最后的兜底选项

#### 4.4.2 主集成机制
仅 Email 和 SMS 渠道支持 Primary 标记：
```typescript
// select-integration.usecase.ts:84
const isChannelSupportsPrimary = CHANNELS_WITH_PRIMARY.includes(command.channelType);
```

用户可通过 API 设置主集成：
```
POST /integrations/:integrationId/set-primary
```

### 4.5 组织级 Kill Switch

**实现文件**：`apps/worker/src/app/workflow/services/standard.worker.ts:131-149`

#### 4.5.1 触发方式
通过 Feature Flag `IS_ORG_KILLSWITCH_FLAG_ENABLED` 控制

#### 4.5.2 行为
Kill Switch 启用时，直接跳过所有任务处理：
```typescript
// standard.worker.ts:141-149
const isKillSwitchEnabled = await this.isKillSwitchEnabled(data);
if (isKillSwitchEnabled) {
  Logger.log(`Kill switch enabled for organizationId ${data._organizationId}. Skipping job.`, LOG_CONTEXT);
  return;
}
```

#### 4.5.3 影响范围
- 仅影响指定组织的消息发送
- 任务仍会被消费，但不执行业务逻辑
- 用于紧急情况下的流量熔断

---

## 五、运营和终端用户暴露的控制点

### 5.1 运营控制面板（API 层）

#### 5.1.1 集成管理 API
| 端点 | 方法 | 功能 |
|------|------|------|
| `/integrations` | GET | 列出所有集成 |
| `/integrations/active` | GET | 列出活跃集成 |
| `/integrations` | POST | 创建集成 |
| `/integrations/:id` | PUT | 更新集成 |
| `/integrations/:id` | DELETE | 删除集成 |
| `/integrations/:id/set-primary` | POST | 设为主集成 |
| `/integrations/:id/auto-configure` | POST | 自动配置 Webhook |

**关键文件**：`apps/api/src/app/integrations/integrations.controller.ts`

#### 5.1.2 健康检查 API
| 端点 | 方法 | 功能 |
|------|------|------|
| `/health-check` | GET | 系统基础设施健康 |
| `/integrations/:id/msteams-health` | GET | MS Teams 集成健康 |
| `/integrations/webhook/provider/:id/status` | GET | Webhook 支持状态 |

#### 5.1.3 限额查询 API
```
GET /integrations/:channelType/limit
```
返回 Novu 内置 Provider 的使用情况：
```typescript
// integrations.controller.ts:428-445
async getProviderLimit(
  @Param('channelType') channelType: ChannelTypeEnum
): Promise<ChannelTypeLimitDto> {
  const result = await this.calculateLimitNovuIntegration.execute(...);
  return result || { limit: 0, count: 0 };
}
```

### 5.2 终端用户控制面板（Dashboard UI）

#### 5.2.1 集成列表页
**文件**：`apps/dashboard/src/components/integrations/components/integrations-list.tsx`
- 展示各渠道集成状态
- 支持启用/禁用切换
- 显示 Primary 标记

#### 5.2.2 集成详情侧边栏
**文件**：`apps/dashboard/src/components/integrations/components/update-integration-sidebar.tsx`
- 凭证配置
- 条件过滤规则配置
- 设为主集成按钮

#### 5.2.3 选择主集成弹窗
**文件**：`apps/dashboard/src/components/integrations/components/modals/select-primary-integration-modal.tsx`
- 当同渠道有多个集成时，引导用户选择主集成
- 影响发送时的默认路由

#### 5.2.4 MS Teams 设置向导
**文件**：`apps/dashboard/src/components/agents/teams-setup-guide.tsx`
- 包含健康检查轮询逻辑
- 实时显示各检查点状态（appRegistration、azureBotCreated、teamsAppCatalog、permissions）

#### 5.2.5 Bridge 健康检查
**文件**：`apps/dashboard/src/hooks/use-fetch-bridge-health-check.ts`
- 检测用户自托管 Bridge 的连接状态
- 影响代码型 Workflow 的执行

### 5.3 消息发送控制点

#### 5.3.1 触发时 Override
发送时可通过 `overrides` 参数动态指定集成：
```typescript
// send-message-email.usecase.ts:92-104
const overrideSelectedIntegration = command.overrides?.email?.integrationIdentifier;
integration = await this.getIntegration({
  identifier: overrideSelectedIntegration as string,
  // ...
});
```

#### 5.3.2 集成条件过滤
集成可配置条件规则，只有满足条件的消息才使用该集成：
```typescript
// select-integration.usecase.ts:41-72
for (const currentIntegration of integrations) {
  const { passed } = await this.conditionsFilter.filter(
    ConditionsFilterCommand.create({ filters: currentIntegration.conditions, ... })
  );
  if (passed) {
    integration = currentIntegration;
    break;
  }
}
```

#### 5.3.3 步骤级停止失败配置
工作流步骤可配置 `shouldStopOnFail`：
```typescript
// standard.worker.ts:258-261
const shouldHaltOnFailure = jobEntity.step?.shouldStopOnFail === undefined ? true : jobEntity.step.shouldStopOnFail;
isLastJobInWorkflow = !hasNextJob || shouldHaltOnFailure;
```

---

## 六、核心数据模型

### 6.1 ExecutionDetails（执行详情）
**Schema**：`libs/dal/src/repositories/execution-details/execution-details.schema.ts`

| 字段 | 用途 |
|------|------|
| `_jobId` | 关联的任务 ID |
| `providerId` | 使用的 Provider |
| `channel` | 渠道类型 |
| `status` | 状态（PENDING/SUCCESS/FAILED） |
| `detail` | 详情枚举（见 `DetailEnum`） |
| `isRetry` | 是否为重试 |
| `raw` | 原始响应/错误数据 |

### 6.2 Integration（集成）
**Schema**：`libs/dal/src/repositories/integration/integration.schema.ts`

| 字段 | 用途 |
|------|------|
| `providerId` | 提供商 ID |
| `channel` | 渠道类型 |
| `active` | 是否启用 |
| `primary` | 是否主集成 |
| `conditions` | 条件过滤规则 |
| `credentials` | 加密存储的凭证 |
| `provisioning` | 部署状态（MS Teams） |

### 6.3 Job（任务）
**Schema**：`libs/dal/src/repositories/job/job.schema.ts`

| 字段 | 用途 |
|------|------|
| `status` | 任务状态 |
| `_parentId` | 父任务 ID |
| `step` | 步骤配置（含 `shouldStopOnFail`） |
| `error` | 错误信息 |

---

## 七、关键代码位置速查表

| 功能 | 文件路径 |
|------|----------|
| 队列健康检测基类 | `libs/application-generic/src/health/queue-health-indicator.service.ts` |
| MS Teams 健康检查 | `apps/api/src/app/integrations/usecases/msteams-health-check/msteams-health-check.usecase.ts` |
| 集成选择逻辑 | `libs/application-generic/src/usecases/select-integration/select-integration.usecase.ts` |
| Worker 失败处理 | `apps/worker/src/app/workflow/services/standard.worker.ts` |
| 指数退避策略 | `apps/worker/src/app/workflow/usecases/webhook-filter-backoff-strategy/webhook-filter-backoff-strategy.usecase.ts` |
| 队列后端降级 | `libs/application-generic/src/services/queues/queue-base.service.ts` |
| Novu Provider 限流 | `libs/application-generic/src/usecases/get-novu-provider-credentials/get-novu-provider-credentials.usecase.ts` |
| 集成管理 API | `apps/api/src/app/integrations/integrations.controller.ts` |
| Kill Switch 检查 | `apps/worker/src/app/workflow/services/standard.worker.ts:131-149` |

---

## 八、总结

Novu 的健康检测与异常降级体系呈现以下特点：

1. **多层防护**：从基础设施（队列/数据库）→ 队列后端（SQS/BullMQ）→ Provider（自定义/Novu托管）→ 组织级（Kill Switch）的四层降级体系
2. **被动检测为主**：主要依赖失败事件触发降级，而非主动探测
3. **手动回切**：大部分降级机制无自动回切，需人工干预或 Feature Flag 控制
4. **细粒度控制**：支持渠道级、集成级、步骤级的多维度控制
5. **全量可观测**：100% 记录发送详情，便于问题追溯和统计分析
