# 站内消息渠道激活判定路径分析报告

## 概述

本文档详细分析站内消息（In-App）渠道从配置录入到真正激活的完整判定路径，覆盖环境开关、租户级配置、订阅者条件以及前端 Widget 鉴权四个核心方面的关联关系。

---

## 一、环境开关层面

### 1.1 集成实体核心字段

集成（Integration）是站内消息渠道配置的载体，核心字段定义于 `libs/dal/src/repositories/integration/integration.entity.ts:20-61`：

| 字段 | 类型 | 说明 |
|------|------|------|
| `active` | `boolean` | 渠道是否激活的核心开关 |
| `connected` | `boolean` | Widget 是否已连接（首次会话初始化时设置） |
| `channel` | `ChannelTypeEnum` | 渠道类型，此处为 `IN_APP` |
| `providerId` | `string` | 服务提供商，站内消息固定为 `InAppProviderIdEnum.Novu` |
| `credentials` | `ICredentialsEntity` | 认证信息，包含 `hmac` 开关等 |
| `conditions` | `StepFilter[]` | 集成级条件过滤器 |

### 1.2 站内消息集成自动创建

在环境初始化时，系统会自动创建 Novu 站内消息集成（`apps/api/src/app/integrations/usecases/create-novu-integrations/create-novu-integrations.usecase.ts:65-80`）：

- 检查当前环境是否已存在 `InAppProviderIdEnum.Novu` 类型的集成
- 若不存在则自动创建，`active` 默认设为 `true`
- 这意味着站内消息渠道在环境创建时默认激活

### 1.3 激活状态判定

在 `select-integration.usecase.ts:83-98` 中，获取集成时的查询条件强制包含：
```typescript
{
  _organizationId: command.organizationId,
  _environmentId: command.environmentId,
  channel: command.channelType,
  active: true,  // 强制过滤激活状态
}
```

> **关键点**：站内消息渠道与其他渠道不同，不使用 `primary` 字段过滤（`CHANNELS_WITH_PRIMARY` 常量中不包含 `IN_APP`）。

---

## 二、租户级配置

### 2.1 集成条件过滤

集成可配置 `conditions` 字段（类型为 `StepFilter[]`），在 `select-integration.usecase.ts:23-73` 中实现租户级条件判定：

1. 首先获取主集成（不考虑条件）
2. 若存在 `tenant` 且有 `userId`，则遍历所有同类型集成
3. 对带有 `conditions` 的集成，通过 `ConditionsFilter` 进行条件匹配
4. 匹配成功的集成将覆盖主集成

条件过滤支持以下类型（`conditions-filter.usecase.ts:343-377`）：
- `TENANT` - 基于租户数据的过滤
- `SUBSCRIBER` - 基于订阅者属性的过滤
- `PAYLOAD` - 基于触发 payload 的过滤
- `IS_ONLINE` - 基于订阅者在线状态
- `PREVIOUS_STEP` - 基于上一步骤执行结果
- `WEBHOOK` - 基于外部 Webhook 回调

### 2.2 工作流租户覆盖

工作流级别支持针对特定租户的偏好覆盖（`get-subscriber-template-preference.usecase.ts:126-146`）：

1. 通过 `tenant.identifier` 查找租户实体
2. 查找 `WorkflowOverride` 中针对该租户和工作流的配置
3. 若存在，则使用其 `preferenceSettings` 覆盖工作流默认设置

### 2.3 偏好覆盖优先级

在 `get-subscriber-template-preference.usecase.ts:27-31` 中定义了偏好覆盖的优先级顺序：

```typescript
const PRIORITY_ORDER = [
  PreferenceOverrideSourceEnum.TEMPLATE,      // 工作流模板配置
  PreferenceOverrideSourceEnum.WORKFLOW_OVERRIDE, // 租户工作流覆盖
  PreferenceOverrideSourceEnum.SUBSCRIBER,    // 订阅者个人设置
];
```

---

## 三、订阅者条件

### 3.1 偏好层级结构

系统支持四个层级的偏好设置，定义于 `merge-preferences.usecase.ts:11-23`：

| 层级 | 类型枚举 | 作用范围 |
|------|----------|----------|
| 1 | `WORKFLOW_RESOURCE` | 工作流资源级，工作流定义时的默认偏好 |
| 2 | `USER_WORKFLOW` | 用户工作流级，用户对特定工作流的偏好 |
| 3 | `SUBSCRIBER_GLOBAL` | 订阅者全局级，订阅者对所有工作流的偏好 |
| 4 | `SUBSCRIBER_WORKFLOW` | 订阅者工作流级，订阅者对特定工作流的偏好 |

### 3.2 偏好合并逻辑

在 `merge-preferences.usecase.ts:47-87` 中实现合并逻辑：

1. 工作流偏好（WORKFLOW_RESOURCE + USER_WORKFLOW）总是被应用
2. 订阅者偏好（SUBSCRIBER_GLOBAL + SUBSCRIBER_WORKFLOW）在以下情况被排除：
   - 工作流设置了 `readOnly: true`（关键通知不可被用户关闭）
   - 调用方显式设置 `excludeSubscriberPreferences: true`
3. 使用 `toMerged` 进行深度合并，后加载的偏好覆盖前者

### 3.3 活跃渠道过滤

在 `get-subscriber-template-preference.usecase.ts:149-207` 中，偏好会被工作流实际包含的渠道过滤：

1. 收集工作流中所有 `active: true` 的步骤
2. 提取这些步骤对应的渠道类型
3. 只保留这些渠道的偏好设置，其他渠道被过滤掉

这意味着：即使订阅者全局开启了站内消息，如果当前工作流不包含站内消息步骤，该偏好也不会生效。

### 3.4 消息发送时的最终判定

在 `send-message.usecase.ts:322-419` 的 `evaluateChannelPreference` 方法中：

1. 调用 `getSubscriberTemplatePreferenceUsecase` 获取合并后的偏好
2. 检查 `preference.enabled`（工作流级开关）
3. 检查 `preference.channels[job.type]`（渠道级开关）
4. 两者都为 `true` 才继续发送

---

## 四、前端 Widget 鉴权

### 4.1 会话初始化流程

Widget 初始化会话的核心逻辑位于 `apps/api/src/app/inbox/usecases/session/session.usecase.ts:118-296`：

**步骤 1：环境验证**
```typescript
const environment = await this.environmentRepository.findEnvironmentByIdentifier(applicationIdentifier);
if (!environment) {
  throw new BadRequestException('Please provide a valid application identifier');
}
```

**步骤 2：站内消息集成检查**
```typescript
const inAppIntegration = await this.selectIntegration.execute(
  SelectIntegrationCommand.create({
    environmentId: environment._id,
    organizationId: environment._organizationId,
    channelType: ChannelTypeEnum.IN_APP,
    providerId: InAppProviderIdEnum.Novu,
    filterData: {},
  })
);
if (!inAppIntegration) {
  throw new NotFoundException('The active in-app integration could not be found');
}
```

**步骤 3：HMAC 验证（如启用）**
```typescript
if (inAppIntegration.credentials.hmac) {
  validateHmacEncryption({
    apiKey: environment.apiKeys[0].key,
    subscriberId: subscriber.subscriberId,
    subscriberHash: command.requestData.subscriberHash,
  });
}
```

**步骤 4：订阅者创建/更新**
- 根据 `subscriberId` 创建或更新订阅者
- `allowUpdate` 字段由 HMAC 验证结果控制，防止未授权修改订阅者信息

**步骤 5：标记为已连接**
```typescript
if (!isOriginFromNovu && !inAppIntegration.connected) {
  await this.integrationRepository.updateOne(
    { _id: inAppIntegration._id },
    { $set: { connected: true } }
  );
}
```

### 4.2 Token 生成与鉴权

会话初始化成功后返回 JWT Token（`session.usecase.ts:233-236`）：
```typescript
const token = await this.authService.getSubscriberWidgetToken(subscriberEntity, contextKeys);
```

后续 Widget API 调用（获取消息列表、标记已读等）都需要携带此 Token，由 `AuthService` 进行验证。

### 4.3 旧版 Widget 初始化

旧版 Widget 初始化逻辑类似，位于 `apps/api/src/app/widgets/usecases/initialize-session/initialize-session.usecase.ts`，核心检查点相同。

---

## 五、完整激活判定链路

### 5.1 配置录入阶段

```
环境创建
    ↓
自动创建 Novu In-App 集成（active: true）
    ↓
工作流配置（添加 In-App 步骤）
    ↓
工作流偏好设置（all.enabled + channels.in_app.enabled）
    ↓
可选：集成条件配置（conditions 字段）
    ↓
可选：租户工作流覆盖（WorkflowOverride）
```

### 5.2 消息触发阶段

```
事件触发
    ↓
SubscriberJobBound：创建/验证订阅者
    ↓
CreateNotificationJobs：为每个步骤创建 Job
    ↓
SendMessage.execute()
    ├─ evaluateStepCondition：检查步骤级条件过滤
    ├─ evaluateChannelPreference：
    │   ├─ 获取工作流活跃步骤的渠道列表
    │   ├─ 合并四层偏好设置
    │   ├─ 过滤出当前渠道的偏好
    │   └─ 检查 workflow.enabled && channel.enabled
    └─ 两者都通过才进入实际发送
```

### 5.3 站内消息发送阶段

```
SendMessageInApp.execute()
    ↓
getIntegration()：调用 SelectIntegration
    ├─ 获取 active: true 的 In-App 集成
    ├─ 若有 tenant，检查集成级 conditions
    └─ 返回匹配的集成
    ↓
若无集成 → 返回 FAILED（SUBSCRIBER_NO_ACTIVE_INTEGRATION）
    ↓
编译模板内容
    ↓
创建/更新 Message 文档
    ↓
通过 WebSocket 推送通知到 Widget
```

### 5.4 Widget 连接阶段

```
Widget 加载
    ↓
调用 /inbox/session 或 /widgets/session/initialize
    ├─ 验证 applicationIdentifier
    ├─ 检查是否存在 active: true 的 In-App 集成
    ├─ 验证 HMAC（如启用）
    ├─ 创建/更新订阅者
    ├─ 首次非 Novu 来源连接时标记 connected: true
    └─ 返回 JWT Token
    ↓
后续 API 调用携带 Token 进行鉴权
```

---

## 六、关键开关汇总

| 层级 | 开关位置 | 字段 | 说明 |
|------|----------|------|------|
| 集成级 | Integration | `active` | 渠道是否激活，创建时默认为 true |
| 集成级 | Integration | `connected` | Widget 是否已实际连接 |
| 集成级 | Integration.credentials | `hmac` | 是否启用 HMAC 鉴权 |
| 工作流级 | Workflow.preferences | `all.enabled` | 工作流总开关 |
| 工作流级 | Workflow.preferences | `channels.in_app.enabled` | 站内消息渠道开关 |
| 工作流级 | Workflow.preferences | `all.readOnly` | 是否为关键通知（不可被订阅者关闭） |
| 工作流级 | Workflow.steps | `active` | 步骤是否激活 |
| 订阅者全局 | SubscriberPreference | `channels.in_app` | 订阅者全局站内消息开关 |
| 订阅者工作流 | SubscriberPreference | `channels.in_app` | 订阅者对特定工作流的站内消息开关 |
| 租户级 | WorkflowOverride | `preferenceSettings` | 租户对工作流的偏好覆盖 |

---

## 七、常见异常场景

### 7.1 工作流已配置但消息未发送

可能原因：
1. 工作流偏好中 `channels.in_app.enabled` 为 false
2. 工作流步骤的 `active` 为 false
3. 订阅者关闭了该渠道的偏好
4. 步骤级条件过滤未通过

### 7.2 Widget 无法初始化

可能原因：
1. `applicationIdentifier` 无效
2. 环境中没有 `active: true` 的站内消息集成
3. HMAC 验证失败（未提供正确的 subscriberHash）
4. 订阅者 ID 格式错误

### 7.3 集成存在但选择失败

可能原因：
1. 集成的 `active` 字段为 false
2. 集成配置了 `conditions` 但当前租户/订阅者不满足条件
3. 集成被软删除（`deleted: true`）

---

## 八、核心代码文件索引

| 功能模块 | 文件路径 |
|----------|----------|
| 集成选择逻辑 | `libs/application-generic/src/usecases/select-integration/select-integration.usecase.ts` |
| 偏好合并逻辑 | `libs/application-generic/src/usecases/merge-preferences/merge-preferences.usecase.ts` |
| 订阅者偏好获取 | `libs/application-generic/src/usecases/get-subscriber-template-preference/get-subscriber-template-preference.usecase.ts` |
| 消息发送主控 | `apps/worker/src/app/workflow/usecases/send-message/send-message.usecase.ts` |
| 站内消息发送 | `apps/worker/src/app/workflow/usecases/send-message/send-message-in-app.usecase.ts` |
| Inbox 会话初始化 | `apps/api/src/app/inbox/usecases/session/session.usecase.ts` |
| Widget 会话初始化 | `apps/api/src/app/widgets/usecases/initialize-session/initialize-session.usecase.ts` |
| 集成实体定义 | `libs/dal/src/repositories/integration/integration.entity.ts` |
| 偏好构建工具 | `packages/shared/src/utils/buildWorkflowPreferences.ts` |
