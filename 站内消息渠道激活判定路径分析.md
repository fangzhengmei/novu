# 站内消息渠道激活判定路径分析报告（修订版）

## 概述

本文档详细分析站内消息（In-App）渠道从配置录入到真正激活的完整判定路径，重点澄清工作流总开关与渠道开关的生效关系，并独立拆分租户条件筛选与 Widget 会话鉴权两条调用链。

---

## 一、环境开关层面

### 1.1 集成实体核心字段

集成（Integration）是站内消息渠道配置的载体，核心字段定义于 `libs/dal/src/repositories/integration/integration.entity.ts:20-61`：

| 字段 | 类型 | 说明 |
|------|------|------|
| `active` | `boolean` | 渠道是否激活的核心开关 |
| `connected` | `boolean` | Widget 是否已连接（首次会话初始化时设置） |
| `channel` | `ChannelTypeEnum` | 渠道类型，此处为 `IN_APP` |
| `providerId` | `string` | 服务提供商，站内消息固定为 `InAppProviderIdEnum.Novu` |
| `credentials` | `ICredentialsEntity` | 认证信息，包含 `hmac` 开关等 |
| `conditions` | `StepFilter[]` | 集成级条件过滤器（用于租户条件筛选） |

### 1.2 站内消息集成自动创建

在环境初始化时，系统会自动创建 Novu 站内消息集成（`apps/api/src/app/integrations/usecases/create-novu-integrations/create-novu-integrations.usecase.ts:65-80`）：

- 检查当前环境是否已存在 `InAppProviderIdEnum.Novu` 类型的集成
- 若不存在则自动创建，`active` 默认设为 `true`
- 这意味着站内消息渠道在环境创建时默认激活

### 1.3 激活状态判定

在 `select-integration.usecase.ts:100-121` 中，获取集成时的查询条件强制包含：
```typescript
{
  _organizationId: command.organizationId,
  _environmentId: command.environmentId,
  channel: command.channelType,
  active: true,  // 强制过滤激活状态
}
```

> **关键点**：站内消息渠道与其他渠道不同，不使用 `primary` 字段过滤（`CHANNELS_WITH_PRIMARY` 常量中不包含 `IN_APP`）。

---

## 二、工作流总开关与渠道开关的真实生效关系

### 2.1 核心结论

**工作流总开关（`all.enabled`）与渠道开关（`channels.in_app.enabled`）不是独立并行判定，而是通过渠道偏好映射落地。**

### 2.2 偏好数据结构

偏好设置采用分层结构，定义于 `packages/shared/src/types/channel.ts`：
```typescript
interface WorkflowPreferences {
  all: {
    enabled: boolean;      // 工作流总开关
    readOnly: boolean;     // 是否为关键通知（不可被订阅者关闭）
  };
  channels: {
    in_app: { enabled: boolean };  // 站内消息渠道开关
    email: { enabled: boolean };
    sms: { enabled: boolean };
    push: { enabled: boolean };
    chat: { enabled: boolean };
  };
}
```

### 2.3 偏好构建与映射逻辑

在 `buildWorkflowPreferences.ts:11-46` 中，`all.enabled` 作为渠道偏好的默认值：

```typescript
const defaultChannelPreference = {
  // Only use the workflow-level enabled preference if defined
  ...(inputPreferences?.all?.enabled !== undefined ? { enabled: inputPreferences.all.enabled } : {}),
};

// 构建每个渠道的偏好
channels: {
  ...defaultPreferences.channels,
  ...Object.values(ChannelTypeEnum).reduce(
    (output, channel) => ({
      ...output,
      [channel]: {
        ...defaultPreferences.channels[channel],
        ...defaultChannelPreference,  // 应用 all.enabled 作为默认值
        ...inputPreferences?.channels?.[channel],  // 渠道级设置覆盖默认值
      },
    }),
    {}
  ),
}
```

**映射规则**：
1. 若未设置 `channels.in_app.enabled`，则使用 `all.enabled` 的值
2. 若已设置 `channels.in_app.enabled`，则以渠道级设置为准
3. 两者都未设置时，使用系统默认值 `true`（`DEFAULT_WORKFLOW_PREFERENCES`）

### 2.4 消息发送时的最终判定

在 `send-message.usecase.ts:536-544` 的 `stepPreferred` 方法中：

```typescript
private stepPreferred(preference: { enabled: boolean; channels: IPreferenceChannels }, job: JobEntity) {
  const workflowPreferred = preference.enabled;  // 注意：此处恒为 true

  const channelPreferred = Object.keys(preference.channels || {}).some(
    (channelKey) => channelKey === job.type && preference.channels?.[job.type]
  );

  return workflowPreferred && channelPreferred;
}
```

**关键澄清**：
- `preference.enabled` 并非来自 `all.enabled`，而是在 `get-subscriber-template-preference.usecase.ts:115` 中被硬编码为 `true`
- 实际生效的只有 `channelPreferred`，即对应渠道的 `enabled` 值
- 工作流级的开关能力已经通过偏好构建阶段被"扁平化"到各个渠道的开关中

### 2.5 关键代码路径验证

```
GetSubscriberTemplatePreference.execute()
    ↓
getSubscriberWorkflowPreference()
    ├─ 调用 getPreferences.safeExecute() 获取合并后的偏好
    ├─ 调用 mapWorkflowPreferencesToChannelPreferences() 将 WorkflowPreferences 转换为 IPreferenceChannels
    └─ 返回 { enabled: true, channels: IPreferenceChannels }  // enabled 硬编码为 true
    ↓
overridePreferences()  // 应用模板、租户覆盖、订阅者设置的优先级
    ↓
stepPreferred()  // 实际只检查 channels[in_app] 是否为 true
```

---

## 三、租户条件筛选调用链

### 3.1 触发前置

**触发时机**：消息发送阶段，在 `SendMessageInApp.execute()` 中调用 `getIntegration()` 时。

**调用入口**：`libs/application-generic/src/usecases/select-integration/select-integration.usecase.ts:20-80`

### 3.2 输入条件

租户条件筛选的触发需要同时满足以下三个条件（`select-integration.usecase.ts:23`）：
```typescript
if (!command.identifier && command.filterData.tenant && command.userId) {
  // 进入租户条件筛选逻辑
}
```

| 条件 | 说明 |
|------|------|
| `!command.identifier` | 不是通过指定 integration identifier 来选择集成 |
| `command.filterData.tenant` | 触发事件时传入了 tenant 信息（string 或包含 identifier 的对象） |
| `command.userId` | 提供了用户 ID（用于条件过滤时的变量解析） |

### 3.3 完整执行流程

```
SelectIntegration.execute()
    ↓
1. getPrimaryIntegration()  // 获取默认集成（不考虑条件）
    ├─ 查询条件：active: true, channel: IN_APP, providerId: Novu
    └─ 按 createdAt 倒序取第一条
    ↓
2. 检查是否满足租户筛选前置条件
    ├─ 不满足 → 直接返回步骤 1 的结果
    └─ 满足 → 进入条件筛选
    ↓
3. 查询当前环境下所有同类型的激活集成
    ↓
4. 解析并获取租户实体（通过 tenant.identifier）
    ↓
5. 遍历所有集成，对带有 conditions 的集成进行条件匹配
    ├─ 调用 normalizeVariablesUsecase 解析变量（注入 tenant 数据）
    └─ 调用 conditionsFilter.filter() 进行条件判定
    ↓
6. 返回第一个条件匹配成功的集成；若无匹配则返回步骤 1 的默认集成
```

### 3.4 条件匹配支持的过滤类型

在 `conditions-filter.usecase.ts:343-377` 中支持以下过滤类型：

| 过滤类型 | 触发字段 | 说明 |
|----------|----------|------|
| 租户属性 | `TENANT` | 基于 tenant.data 中的字段进行匹配 |
| 订阅者属性 | `SUBSCRIBER` | 基于 subscriber.data 中的字段进行匹配 |
| 触发 Payload | `PAYLOAD` | 基于事件触发时传入的 payload 进行匹配 |
| 在线状态 | `IS_ONLINE` | 基于订阅者的 isOnline 字段 |
| 在线时长 | `IS_ONLINE_IN_LAST` | 基于订阅者最后在线时间 |
| 上一步结果 | `PREVIOUS_STEP` | 基于工作流中前一步骤的执行结果 |
| 外部 Webhook | `WEBHOOK` | 调用外部 Webhook 获取判定结果 |

### 3.5 SelectIntegration 调用链中各过滤类型的可用边界

#### 3.5.1 调用链上下文分析

在 `select-integration.usecase.ts:58-66` 中，调用 `ConditionsFilter` 时传入的上下文为：
```typescript
ConditionsFilterCommand.create({
  filters: currentIntegration.conditions,
  environmentId: command.environmentId,
  organizationId: command.organizationId,
  userId: command.userId,
  variables,    // 仅包含 tenant（来自 filterData.tenant）
  // 注意：此处未传入 job 和 step！
})
```

**关键缺失**：`job`（工作流任务上下文）和 `step`（步骤上下文）均未传入。

#### 3.5.2 过滤类型可用性分类

| 分类 | 过滤类型 | 可用程度 | 核心原因 |
|------|----------|----------|----------|
| **可直接生效** | `TENANT` | ✅ 完全可用 | 仅依赖 `variables.tenant`，在 SelectIntegration 中显式传入 |
| **需额外上下文才可靠** | `SUBSCRIBER` | ⚠️ 部分可用 | 依赖 `command.job` 来获取 subscriberId 并查询订阅者，但 job 未传入 |
| **需额外上下文才可靠** | `PAYLOAD` | ⚠️ 部分可用 | 依赖 `command.job?.payload`，但 job 未传入 |
| **不适合直接依赖** | `IS_ONLINE` | ❌ 不可靠 | `processIsOnline()` 直接访问 `command.job.subscriberId`（第 193 行），job 不存在会抛异常 |
| **不适合直接依赖** | `IS_ONLINE_IN_LAST` | ❌ 不可靠 | 同上，依赖 `command.job.subscriberId` |
| **不适合直接依赖** | `PREVIOUS_STEP` | ❌ 不可靠 | `processPreviousStep()` 访问 `command.job.transactionId`（第 141 行），job 不存在会抛异常 |
| **需额外上下文才可靠** | `WEBHOOK` | ⚠️ 部分可用 | 可调用但 `compileFilter` 时缺少 job 上下文，模板渲染可能失败 |

---

#### 3.5.3 可直接生效的过滤类型

**`TENANT` 类型**

- **可用依据**：在 `normalize-variables.usecase.ts:73-93` 中，tenant 既可以通过 `command.variables?.tenant` 获取（在 SelectIntegration 中已传入），也可以通过 `command.job?.tenant` 获取。由于 SelectIntegration 已显式传入 tenant，因此该类型完全可用。
- **匹配逻辑**：`processFilterEquality(variables, child, ...)` → 通过 `_.get(variables, 'tenant.data.fieldName')` 取值
- **失败表现**：字段不存在时 `actualValue` 为 `undefined`，除 `IS_DEFINED` 运算符外都会判定为不通过
- **排障提示**：
  1. 检查 `integration.conditions[].children[].field` 是否与 `tenant.data` 中的字段名一致
  2. 检查触发事件时传入的 tenant.identifier 是否正确
  3. 检查租户实体的 data 字段是否包含预期的属性

---

#### 3.5.4 需额外上下文才可靠的过滤类型

**`SUBSCRIBER` 类型**

- **问题根源**：在 `normalize-variables.usecase.ts:45-67` 中，`fetchSubscriberIfMissing()` 需要满足两个条件才会查询订阅者：
  ```typescript
  if (subscriberFilterExist && command.job) {
    // 查询订阅者...
  }
  ```
  由于 SelectIntegration 未传入 job，即使集成配置了 SUBSCRIBER 过滤，`command.variables.subscriber` 也始终为 `undefined`。
- **失败表现**：`processFilterEquality` 中 `_.get(variables, 'subscriber.data.fieldName')` 返回 `undefined`，条件判定不通过，导致集成无法被选中
- **排障提示**：
  1. 若必须使用 SUBSCRIBER 过滤，需要在调用 SelectIntegration 前将 subscriber 注入 variables
  2. 或者在触发事件时确保 tenant + userId 组合能正确路由到目标集成
  3. 调试时查看 FilterProcessingDetails 中的 actualValue 是否为 undefined

**`PAYLOAD` 类型**

- **问题根源**：在 `normalize-variables.usecase.ts:35-37` 中：
  ```typescript
  filterVariables.payload = command.variables?.payload
    ? command.variables?.payload
    : (command.job?.payload ?? undefined);
  ```
  SelectIntegration 中 `command.variables` 仅包含 tenant，因此 payload 始终为 `undefined`。
- **失败表现**：所有 PAYLOAD 类型的过滤都会因值为 undefined 而不通过
- **排障提示**：
  1. SelectIntegration 调用链中不适合使用 PAYLOAD 过滤
  2. 若需基于 payload 选择集成，应将关键信息下沉到 tenant.data 中

**`WEBHOOK` 类型**

- **问题根源**：在 `conditions-filter.usecase.ts:351-356` 中，Webhook 过滤会先调用 `compileFilter(child.value, variables, command.job)` 对 Webhook URL 进行模板渲染。由于 job 未传入，若 URL 模板中引用了 `{{job.*}}` 变量会导致渲染失败。
- **失败表现**：
  - 若 URL 模板不依赖 job 变量 → 可正常调用并返回结果
  - 若 URL 模板依赖 job 变量 → `compileFilter` 抛异常，导致整个过滤流程失败
- **排障提示**：
  1. 在集成条件中使用 Webhook 过滤时，确保 URL 模板仅引用 `{{tenant.*}}` 等可用变量
  2. 检查 Webhook 服务是否可被 Novu 网络访问
  3. 查看 execution details 确认是否有模板渲染错误

---

#### 3.5.5 不适合在该链路直接依赖的过滤类型

**`IS_ONLINE` / `IS_ONLINE_IN_LAST` 类型**

- **问题根源**：在 `conditions-filter.usecase.ts:187-195` 中：
  ```typescript
  private async processIsOnline(filter, command, filterProcessingDetails) {
    const subscriber = await this.getSubscriberBySubscriberId({
      subscriberId: command.job.subscriberId,  // ❌ 直接访问 command.job
      _environmentId: command.environmentId,
    });
    // ...
  }
  ```
  代码未对 `command.job` 做存在性检查，直接访问 `command.job.subscriberId` 会抛出 `Cannot read properties of undefined` 异常。
- **失败表现**：消息发送失败，worker 日志中出现 TypeError
- **排障提示**：
  1. **绝对不要**在集成条件（integration.conditions）中使用 IS_ONLINE 或 IS_ONLINE_IN_LAST 过滤
  2. 此类过滤应放在工作流步骤级别的 filters 中（step.filters），那里有完整的 job 上下文
  3. 若已错误配置，需要更新集成配置移除这些条件

**`PREVIOUS_STEP` 类型**

- **问题根源**：在 `conditions-filter.usecase.ts:135-146` 中：
  ```typescript
  private async processPreviousStep(filter, command, filterProcessingDetails) {
    const job = await this.jobRepository.findOne({
      transactionId: command.job.transactionId,  // ❌ 直接访问 command.job
      _subscriberId: command.job._subscriberId,
      // ...
    });
    // ...
  }
  ```
  同样直接访问 `command.job` 的属性，缺少空值检查。
- **失败表现**：消息发送失败，worker 日志中出现 TypeError
- **排障提示**：
  1. **绝对不要**在集成条件中使用 PREVIOUS_STEP 过滤
  2. 此类过滤仅适用于工作流内部的步骤级过滤
  3. 若已配置，需要通过 API 更新集成的 conditions 字段

---

#### 3.5.6 安全使用建议

1. **推荐使用**：`TENANT` 类型是集成条件过滤的首选，也是唯一在 SelectIntegration 链路中完全可靠的类型
2. **谨慎使用**：`SUBSCRIBER`、`PAYLOAD`、`WEBHOOK` 类型在特定场景下可用，但需要注意上下文缺失问题
3. **禁止使用**：`IS_ONLINE`、`IS_ONLINE_IN_LAST`、`PREVIOUS_STEP` 类型绝对不要用于集成条件，会直接导致运行时异常
4. **调试技巧**：在 `ConditionsFilter.filter()` 返回结果中包含 `conditions` 数组，可通过 execution details 查看每个过滤条件的 actualValue 和 passed 状态

### 3.6 失败分支

| 场景 | 行为 | 结果 |
|------|------|------|
| 无任何满足 `active: true` 的集成 | 直接返回 `undefined` | 消息发送失败，错误码 `SUBSCRIBER_NO_ACTIVE_INTEGRATION` |
| 有默认集成但无条件匹配的集成 | 返回默认集成 | 消息可正常发送 |
| 条件匹配过程中抛出异常（如 IS_ONLINE 无 job） | 向上抛出异常 | 消息发送失败，worker 日志出现 TypeError |
| tenant.identifier 不存在 | 不进行条件筛选，返回默认集成 | 消息可正常发送 |
| SUBSCRIBER/PAYLOAD 过滤值为 undefined | 条件判定不通过，跳过该集成 | 继续尝试其他集成或使用默认 |
| 多个集成都匹配 conditions | 返回第一个匹配的集成（按 createdAt 倒序遍历） | 消息使用该集成发送 |

---

## 四、Widget 会话鉴权调用链

### 4.1 触发前置

**触发时机**：前端 Widget 初始化时，用户打开包含通知中心的页面。

**两个独立入口**：
1. 新版 Inbox：`POST /inbox/session` → `apps/api/src/app/inbox/usecases/session/session.usecase.ts`
2. 旧版 Widget：`POST /widgets/session/initialize` → `apps/api/src/app/widgets/usecases/initialize-session/initialize-session.usecase.ts`

### 4.2 输入条件

#### 新版 Inbox 会话（`session.usecase.ts:119-296`）

| 参数 | 必填 | 说明 |
|------|------|------|
| `applicationIdentifier` | 是 | 环境标识符，用于定位环境 |
| `subscriber` / `subscriberId` | 是 | 订阅者唯一标识 |
| `subscriberHash` | 可选 | HMAC 签名，集成启用 HMAC 时必填 |
| `context` / `contextHash` | 可选 | 多租户上下文及签名 |
| `firstName` / `lastName` / `email` / `phone` | 可选 | 订阅者属性，用于创建/更新 |

#### 旧版 Widget 会话（`initialize-session.usecase.ts:33-86`）

| 参数 | 必填 | 说明 |
|------|------|------|
| `applicationIdentifier` | 是 | 环境标识符 |
| `subscriberId` | 是 | 订阅者唯一标识 |
| `hmacHash` | 可选 | HMAC 签名，集成启用 HMAC 时必填 |
| `firstName` / `lastName` / `email` / `phone` | 可选 | 订阅者属性 |

### 4.3 完整执行流程（新版 Inbox）

```
Session.execute()
    ↓
1. validateRequestData()  // 校验请求数据格式
    └─ 失败 → BadRequestException: "A valid application identifier is required..."
    ↓
2. 获取 applicationIdentifier
    ↓
3. 环境验证：findEnvironmentByIdentifier()
    └─ 失败 → BadRequestException: "Please provide a valid app identifier"
    ↓
4. 站内消息集成检查：selectIntegration.execute()
    ├─ 查询条件：channel=IN_APP, providerId=Novu, active=true, filterData={}
    └─ 失败 → NotFoundException: "The active in-app integration could not be found"
    ↓
5. HMAC 验证（如集成启用了 credentials.hmac）
    ├─ 验证 subscriberHash
    │   └─ 失败 → BadRequestException: "Please provide a valid HMAC hash"
    └─ 验证 contextHash（如提供 context）
        └─ 失败 → BadRequestException
    ↓
6. 解析 contextKeys（如提供 context）
    ↓
7. 创建或更新订阅者：createOrUpdateSubscriber.execute()
    ├─ allowUpdate 由 HMAC 验证结果控制
    └─ 失败 → 向上抛出异常
    ↓
8. 标记集成已连接（首次非 Novu 来源连接时）
    └─ 更新 integration.connected = true
    ↓
9. 生成 JWT Token：authService.getSubscriberWidgetToken()
    ↓
10. 返回会话信息（含 token、unreadCount、profile 等）
```

### 4.4 失败分支汇总

| 阶段 | 失败场景 | HTTP 状态码 | 错误信息 |
|------|----------|-------------|----------|
| 请求校验 | subscriberId 缺失但提供了 applicationIdentifier | 422 | "Subscriber ID is required" |
| 环境验证 | applicationIdentifier 无效 | 400 | "Please provide a valid app identifier" |
| 集成检查 | 无 active=true 的站内消息集成 | 404 | "The active in-app integration could not be found" |
| HMAC 验证 | 集成启用 HMAC 但未提供 subscriberHash | 400 | "Please provide a valid HMAC hash" |
| HMAC 验证 | subscriberHash 验证失败 | 400 | "Please provide a valid HMAC hash" |
| 订阅者操作 | 订阅者创建/更新失败 | - | 向上抛出底层异常 |

### 4.5 与租户条件筛选的本质区别

| 维度 | Widget 会话鉴权 | 租户条件筛选 |
|------|----------------|--------------|
| **触发时机** | Widget 前端初始化时 | 后端消息发送时 |
| **核心目的** | 验证环境、集成状态，颁发访问 Token | 根据租户属性选择合适的集成实例 |
| **输入来源** | 前端 Widget 传入 | 后端事件触发时传入 |
| **失败影响** | Widget 无法加载，用户看不到通知中心 | 消息发送失败或使用默认集成 |
| **租户关联** | 通过 context 传递租户信息（可选） | 通过 filterData.tenant 传递（必须） |
| **条件判定** | 仅检查集成是否 active | 支持复杂的条件过滤（tenant/data/payload 等） |

> **重要**：两者是完全独立的链路，Widget 鉴权通过不代表消息发送时的租户条件筛选也能通过；反之亦然。

---

## 五、完整激活判定链路

### 5.1 配置录入阶段

```
环境创建
    ↓
自动创建 Novu In-App 集成（active: true）
    ↓
工作流配置
    ├─ 添加 In-App 步骤（active: true）
    └─ 设置偏好：all.enabled + channels.in_app.enabled（通过 UI 或 API）
    ↓
可选：集成条件配置（conditions 字段，支持 tenant 过滤）
    ↓
可选：租户工作流覆盖（WorkflowOverride.preferenceSettings）
    ↓
可选：订阅者偏好设置（全局 or 工作流级）
```

### 5.2 消息触发阶段

```
事件触发（triggerEvent）
    ↓
SubscriberJobBound：创建/验证订阅者
    ↓
CreateNotificationJobs：为每个步骤创建 Job
    ↓
SendMessage.execute()
    ├─ evaluateStepCondition：检查步骤级条件过滤（filters）
    │   └─ 不通过 → SKIPPED
    └─ evaluateChannelPreference：
        ├─ 获取工作流活跃步骤的渠道列表（过滤掉未激活步骤的渠道）
        ├─ 合并四层偏好设置（工作流资源→用户→订阅者全局→订阅者工作流）
        ├─ 检查 readOnly 标志（决定是否排除订阅者偏好）
        ├─ 映射为 IPreferenceChannels（all.enabled 已扁平化到各渠道）
        ├─ 应用优先级覆盖（模板→租户覆盖→订阅者）
        └─ 检查 channels[in_app] 是否为 true
            └─ 不通过 → SKIPPED
    ↓
两者都通过 → 进入实际发送
```

### 5.3 站内消息发送阶段

```
SendMessageInApp.execute()
    ↓
getIntegration()：调用 SelectIntegration
    ├─ 获取默认集成（active: true, channel: IN_APP）
    ├─ 若有 tenant 且有 userId → 进行集成条件筛选
    │   ├─ 遍历所有同类型激活集成
    │   ├─ 对带 conditions 的集成进行条件匹配
    │   └─ 匹配成功则使用该集成
    ├─ 无匹配集成 → 使用默认集成
    └─ 无任何集成 → 返回 FAILED（SUBSCRIBER_NO_ACTIVE_INTEGRATION）
    ↓
编译模板内容
    ↓
创建/更新 Message 文档
    ↓
通过 WebSocket 推送通知到 Widget
```

### 5.4 Widget 连接阶段

```
Widget 前端加载
    ↓
调用 /inbox/session 或 /widgets/session/initialize
    ├─ 验证 applicationIdentifier → 环境存在
    ├─ 检查是否存在 active: true 的 In-App 集成
    ├─ 验证 HMAC（如启用）
    ├─ 创建/更新订阅者
    ├─ 首次非 Novu 来源连接时标记 integration.connected = true
    └─ 返回 JWT Token
    ↓
后续 API 调用（获取消息列表、标记已读等）携带 Token 进行鉴权
```

---

## 六、关键开关汇总

| 层级 | 开关位置 | 字段 | 说明 | 生效阶段 |
|------|----------|------|------|----------|
| 集成级 | Integration | `active` | 渠道是否激活，创建时默认为 true | 集成选择、Widget 鉴权 |
| 集成级 | Integration | `connected` | Widget 是否已实际连接 | 前端展示用，不影响发送 |
| 集成级 | Integration.credentials | `hmac` | 是否启用 HMAC 鉴权 | Widget 会话初始化 |
| 集成级 | Integration | `conditions` | 集成级条件过滤器 | 消息发送时的租户筛选 |
| 工作流级 | Workflow.preferences | `all.enabled` | 工作流总开关（作为渠道默认值） | 偏好构建阶段 |
| 工作流级 | Workflow.preferences | `channels.in_app.enabled` | 站内消息渠道开关 | 消息发送时的 stepPreferred |
| 工作流级 | Workflow.preferences | `all.readOnly` | 是否为关键通知（不可被订阅者关闭） | 偏好合并阶段 |
| 工作流级 | Workflow.steps | `active` | 步骤是否激活 | 活跃渠道过滤 |
| 订阅者全局 | SubscriberPreference | `channels.in_app` | 订阅者全局站内消息开关 | 偏好合并阶段 |
| 订阅者工作流 | SubscriberPreference | `channels.in_app` | 订阅者对特定工作流的站内消息开关 | 偏好合并阶段 |
| 租户级 | WorkflowOverride | `preferenceSettings` | 租户对工作流的偏好覆盖 | 偏好覆盖阶段 |

---

## 七、常见异常场景

### 7.1 工作流已配置但消息未发送

可能原因：
1. 工作流偏好中 `channels.in_app.enabled` 为 false（或被 all.enabled 默认为 false）
2. 工作流 In-App 步骤的 `active` 为 false（被排除在活跃渠道外）
3. 订阅者关闭了该渠道的偏好（且未设置 readOnly）
4. 步骤级条件过滤（step.filters）未通过
5. 工作流未设置 In-App 步骤（活跃渠道列表中无 in_app）

### 7.2 Widget 无法初始化

可能原因：
1. `applicationIdentifier` 无效或拼写错误
2. 环境中没有 `active: true` 的站内消息集成（可能被手动停用）
3. HMAC 验证失败：
   - 集成启用了 HMAC 但前端未传入 subscriberHash
   - subscriberHash 计算错误（使用了错误的密钥或算法）
4. 订阅者 ID 格式错误或缺失
5. 环境被删除或停用

### 7.3 集成存在但选择失败

可能原因：
1. 集成的 `active` 字段被手动设置为 false
2. 集成配置了 `conditions` 但当前租户/订阅者不满足条件
3. 集成被软删除（`deleted: true`）
4. 集成的 `providerId` 不是 `InAppProviderIdEnum.Novu`

### 7.4 多租户场景下集成选择不符合预期

可能原因：
1. 触发事件时未传入 `tenant` 参数
2. 未传入 `userId` 参数（两个条件缺一不可）
3. 集成的 `conditions` 配置错误（字段名拼写、运算符使用等）
4. 租户实体不存在（tenant.identifier 无效）
5. 多个集成都配置了 conditions，第一个匹配的集成被选中（非预期）

---

## 八、核心代码文件索引

| 功能模块 | 文件路径 |
|----------|----------|
| 偏好构建工具 | `packages/shared/src/utils/buildWorkflowPreferences.ts` |
| 默认偏好常量 | `packages/shared/src/consts/preferences/preferences.const.ts` |
| 集成选择逻辑 | `libs/application-generic/src/usecases/select-integration/select-integration.usecase.ts` |
| 偏好合并逻辑 | `libs/application-generic/src/usecases/merge-preferences/merge-preferences.usecase.ts` |
| 订阅者偏好获取 | `libs/application-generic/src/usecases/get-subscriber-template-preference/get-subscriber-template-preference.usecase.ts` |
| 偏好获取入口 | `libs/application-generic/src/usecases/get-preferences/get-preferences.usecase.ts` |
| 条件过滤器 | `libs/application-generic/src/usecases/conditions-filter/conditions-filter.usecase.ts` |
| 消息发送主控 | `apps/worker/src/app/workflow/usecases/send-message/send-message.usecase.ts` |
| 站内消息发送 | `apps/worker/src/app/workflow/usecases/send-message/send-message-in-app.usecase.ts` |
| Inbox 会话初始化 | `apps/api/src/app/inbox/usecases/session/session.usecase.ts` |
| Widget 会话初始化 | `apps/api/src/app/widgets/usecases/initialize-session/initialize-session.usecase.ts` |
| 集成实体定义 | `libs/dal/src/repositories/integration/integration.entity.ts` |
